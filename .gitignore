node_modules/
dist/
.env
.env.local
.env.production
*.log
.DS_Store
.turbo
coverage/
__pycache__/
*.pyc
*.egg-info/
.venv/
build/
*.whl
*.tar.gz
strale-frontend/

# Claude Code: ignore local-only state + per-machine settings, but allow
# project skills/commands/protocol docs to be tracked. Skills are project
# knowledge (vendor-switch checklist, /go, /end-session, etc.) — they
# should be available to every Claude Code session, not just Petter's
# local machine.
.claude/state/
.claude/worktrees/
.claude/settings.local.json
.claude/*.local.*

# MCP publisher and registry tokens (CRIT-S43: previously corrupted as UTF-16)
mcp-publisher.exe
.mcpregistry_github_token
.mcpregistry_registry_token

apps/spike/

# Raw sweep evidence (may embed production output values) stays local
audit-output/*.jsonl
# Scratch produced by review/audit sessions. CLAUDE.md routes anything worth
# keeping to archive/sessions/; the root stays clean. Ignored rather than
# committed so the mutation-test guard's clean-tree check (which counts
# untracked files, deliberately) is not permanently red.
audit-output/
review-bundle/
.codex-shell-marker
# Throwaway prod-query scripts. Review sessions write these into the shared
# checkout while other sessions work in it, so they show up as untracked files
# in someone else's tree and block the mutation-test guard's clean-tree check
# for reasons that have nothing to do with the change under test.
apps/api/scripts/_tmp*
docs/company/ceo-dashboard.html

# Guarded production write credential - never committed, never auto-loaded.
# The .template beside it is committed; the real file is not.
.env.operator-write
