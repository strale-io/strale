{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://strale.dev/schemas/protocol-coverage.schema.json",
  "title": "Protocol coverage manifest",
  "description": "T6 M3 batch 7. Maps every mandatory protocol/rule currently governing this repository to its trigger, full-body path under docs/governance/protocols/ (or, for production authority, the governing CHARTER.md section), governing decision, decision record, and enforcing code/tests. authority_active stays false and the router generated from this file stays inactive until the founder-gated M4 cutover. Checked by scripts/protocol-coverage-lib.mjs (npm run protocols:coverage). No row here changes any protocol's substance -- CLAUDE.md remains the sole authority for the ten mirrored rows; docs/company/CHARTER.md remains the authority for production authority.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "authority_active", "protocols"],
  "properties": {
    "schema_version": { "const": 1 },
    "authority_active": {
      "const": false,
      "description": "This manifest and the router generated from it carry no authority until the founder-gated M4 cutover."
    },
    "protocols": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/protocolRow" }
    }
  },
  "$defs": {
    "protocolRow": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "trigger", "full_body", "source", "decision", "decision_record", "enforced_by"],
      "allOf": [
        {
          "if": { "properties": { "decision": { "const": "none" } } },
          "then": { "required": ["decision_reason"] },
          "else": { "not": { "required": ["decision_reason"] } }
        },
        {
          "if": { "properties": { "decision_record": { "type": "null" } } },
          "then": { "required": ["decision_record_note"] },
          "else": { "not": { "required": ["decision_record_note"] } }
        },
        {
          "if": { "properties": { "enforced_by": { "maxItems": 0 } } },
          "then": { "required": ["enforced_by_note"] },
          "else": { "not": { "required": ["enforced_by_note"] } }
        }
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
          "description": "Stable kebab-case protocol id, used as the router's anchor and never renamed once a row exists."
        },
        "name": { "type": "string", "minLength": 1 },
        "trigger": {
          "type": "string",
          "minLength": 1,
          "description": "One sentence: when a session must load this protocol's full body, drawn from the protocol's own trigger or scope text."
        },
        "full_body": {
          "type": "string",
          "minLength": 1,
          "description": "Repository path to the protocol's complete text. Must exist."
        },
        "source": {
          "type": "string",
          "minLength": 1,
          "description": "Where full_body's text is copied from: a CLAUDE.md heading (for the ten mirrored rows) or another document's section heading (production authority: a docs/company/CHARTER.md heading)."
        },
        "decision": {
          "type": "string",
          "minLength": 1,
          "description": "The governing decision id (e.g. DEC-20260320-B), or the literal \"none\" when no numbered decision governs this protocol."
        },
        "decision_reason": {
          "type": "string",
          "minLength": 1,
          "description": "Required when decision is \"none\": why no decision id governs this protocol."
        },
        "decision_record": {
          "type": ["string", "null"],
          "description": "Path under docs/decisions/records/ for the governing decision, or null when no formal record file exists for it."
        },
        "decision_record_note": {
          "type": "string",
          "minLength": 1,
          "description": "Required when decision_record is null: where the decision is recorded instead (CLAUDE.md and/or Notion)."
        },
        "enforced_by": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Repository paths of the code, scripts and tests that enforce this protocol. Each must exist. May be empty only alongside enforced_by_note."
        },
        "enforced_by_note": {
          "type": "string",
          "minLength": 1,
          "description": "Required when enforced_by is empty: states the protocol is enforced by review alone."
        }
      }
    }
  }
}
