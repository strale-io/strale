---
record_key: DEC-20260420-K--notion-34867c87082c8198b6ecf3569a68a9b4
id: DEC-20260420-K
title: 'Session B Open Questions resolved: OQ-1 hybrid split ownership, OQ-2 5-second countdown + CI=1 skip, OQ-3 defer solutions YAML pathway, OQ-4 auto-derive solution PII from step union, OQ-5 delete env-var kill switch + add --skip-gates=<reason> CLI flag, OQ-6 delete transparency-tag heuristic default (same pattern as SA.2b.d). OQ-7 already being fixed in three-hotfix prompt.'
status: active
topic: session-b-open-questions-resolved
scope: product
owner: petter
decided_at: 2026-04-20
relations:
  - type: related_to
    target: DEC-20260420-D--notion-34867c87082c81f0827eedf29d133600
  - type: related_to
    target: DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6
  - type: related_to
    target: DEC-20260420-J--notion-34867c87082c81478c15cb6985d10137
evidence:
  - https://app.notion.com/34867c87082c8198b6ecf3569a68a9b4
  - apps/api/scripts/onboard.ts
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

Pins six of seven Session B Open Questions (OQ-7 already resolved
elsewhere): **OQ-1**, hybrid split field ownership between manifest and DB
("manifest-canonical" fields overwrite on backfill; "DB-canonical" fields
never do); **OQ-2**, a 5-second `--force` countdown with `CI=1` skip;
**OQ-3**, defer a solutions-YAML third-party authoring pathway; **OQ-4**,
auto-derive a solution's PII footprint as the union of its steps' PII
rather than a separately-declared field; **OQ-5**, delete the
`SKIP_ONBOARDING_GATES` env var, replace with a per-invocation
`--skip-gates=<reason>` CLI flag; **OQ-6**, delete the `transparency_tag`
heuristic default, require explicit declaration.

## Context

The row's own OQ-1 field-ownership split names the exact fields on each
side: manifest-canonical includes `slug, name, description, category,
input_schema, output_schema, limitations, maintenance_class,
processes_personal_data, personal_data_categories, data_source,
data_source_type`; DB-canonical includes `price_cents, is_free_tier,
freshness_category, transparency_tag, geography, lifecycle_state, visible,
matrix_sqs and trust columns, last_tested_at, created_at, updated_at`. The
row states this "resolves trivially" the 238-slug Class 4 drift "from
DEC-20260420-H." This names the exact subject of
`DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6` ("Option C for
238-slug manifest drift"), so the relation above targets that specific
qualified record, not its "Strale positioning and ICP clarification"
sibling.

The row's own References section states: "DEC-20260420-D: SA.2b PII
classification framework (OQ #6 there = delete heuristic, precedent for
OQ-6 here)... DEC-20260420-H: Manifest drift audit Option C defer to
Session B... DEC-20260420-J: SA.2b.d closes Session A, establishes
declarative-over-heuristic precedent." `DEC-20260420-D` has a single
formal record, targeted directly.
`DEC-20260420-J`'s exact title ("SA.2b.d closes Session A...") matches
this same batch's `DEC-20260420-J--notion-34867c87082c81478c15cb6985d10137`
record word-for-word, so the relation targets that record specifically,
not its documented-only superseded sibling.

OQ-2's design is stated as following "Standard Unix pattern (rm -rf,
rsync --delete)." OQ-5's replacement design ("`--skip-gates=<reason>`...
goes into the onboarding audit log (F-B-011 when implemented)") and OQ-6's
("Cluster 2") both reference an onboarding-engine restructure the row
calls "Cluster 2" without naming it via a Decision ID; those references are
recorded here as prose (the plan itself is tracked in this repository at
`archive/sessions/audit-reports/cluster_2_design.md`, not as a Decision
record).

This historical ID is reused by a second, unrelated Notion row (the Payee
Assurance bank-verification launch-gate decision, a product-launch decision
made the same day) and was an unresolved registry collision until this
batch; the two rows are resolved together in
`archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-K.md`
with distinct source-qualified record keys. This record covers only the
Session B Open Questions row.

The historical Notion scope field on this row was `feature`. It is
recorded here as `scope: product`: it locks product/process decisions
(field ownership, operator UX, PII propagation rules) ahead of an
engineering restructure, rather than shipping an implementation itself.

## Rationale

The rationale, as the row states it per open question: OQ-1's hybrid split
"formalizes what's already happening in production" and avoids either
rebuilding the admin UI (manifest-canonical) or reducing manifests to
"decorative authoring hints" (DB-canonical); OQ-4's auto-derive is
"Mathematical fact, not product decision"; OQ-5's CLI flag makes bypass
"Visible in command history, shell logs, CI traces" where the env var was
"silent bypass with no telemetry"; OQ-6 applies "Exact same anti-pattern as
F-A-003 which was deleted in SA.2b.d," for consistency.

## Consequences

Status verified on 2026-09-05, against `main`.

**OQ-1's field-ownership split is present in `apps/api/scripts/onboard.ts`
today, evolved past this row's original framing.** The script carries
authority-drift detection logic (comments reference "authority decisions
(hybrid: fill-null-only; manifest-drift: throw)") and a
`--force-override-authority` interactive guard ("Cluster 2 Phase 4a"),
gated to interactive TTY sessions and refused in `--batch` mode, a
stricter mechanism than this row's own OQ-2 countdown design, addressing
the same underlying authority-boundary problem this row's split was meant
to formalize.

**OQ-6's heuristic deletion is corroborated by the sibling SA.2b.d record's
own text** (`DEC-20260420-J--notion-34867c87082c81478c15cb6985d10137`):
the `detectPersonalData` heuristic was deleted the same day this row cites
as precedent; whether `detectTransparencyTag` (this row's own OQ-6 subject)
was separately deleted is not independently confirmed in this pass, beyond
the manifest evidence that all 342 manifests declare a `data_source_type`
field consistent with explicit-declaration-over-heuristic having become the
norm.

**OQ-3's deferral trigger has not yet fired on the evidence available.**
The row's own revisit trigger ("solution count crosses ~150, OR a
third-party contribution request arrives") is not verified against a
current solution count in this pass; `docs/programs/README.md` and this
batch's other evidence do not establish whether that threshold has been
crossed.

## Reversal conditions

None recorded on the row (`Superseded By` and `Outcome` are both null). The
row's own OQ-3 names its own revisit trigger; no other reversal condition
is stated.
