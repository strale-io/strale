---
record_key: DEC-20260420-I--notion-34867c87082c8172a41ac4c9d52904de
id: DEC-20260420-I
title: 'SA.2b.c direct-SQL backfill of 260 remaining capabilities with PII classification + 2 Class 2 fixture fixes. Complete.'
status: active
topic: sa2bc-direct-sql-pii-backfill
scope: technical
owner: petter
decided_at: 2026-04-20
relations:
  - type: related_to
    target: DEC-20260420-D--notion-34867c87082c81f0827eedf29d133600
evidence:
  - https://app.notion.com/34867c87082c8172a41ac4c9d52904de
  - manifests/estonian-company-data.yaml
  - manifests/spanish-company-data.yaml
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

Complete SA.2b.c: a Phase 1 classification proposal of 292 rows (89 TRUE,
203 FALSE post-correction, 0 uncertain), 9 chat-review corrections applied
(behavioral-to-financial reclassification for ENS/wallet capabilities,
`postal-code-lookup` and `gdpr-fine-lookup` flipped to FALSE, `email-draft`
gains `professional`, 31 text-processing slugs reverted from a conservative
TRUE to FALSE), then a Phase 2 execution of 23 grouped `UPDATE` statements
against 260 capability rows, all counts matching expected sizes exactly.
Bundled two Class 2 fixture fixes (estonian-company-data, spanish-company-data)
in the same session.

## Context

The row states its own key classification principle, applied during chat
review to correct an initial over-broad stance: "'processes PII' means the
capability's inherent behavior involves PII, not 'user might provide PII in
arbitrary text.'" This corrected 31 text-processing capabilities (csv-clean,
pdf-extract, summarize, translate, etc.) from an initial conservative TRUE
to FALSE, on the reasoning that "the user is the data controller" for
purposeful text-in/text-out processing. The row also documents specific
category reassignments: wallet capabilities from `[behavioral, financial]`
to `[financial]` (wallet activity is transactional, not inferred-trait
behavioral); ENS capabilities from `[behavioral]` to `[name, financial]`
(ENS names are user-chosen handles mapped to wallet addresses).

The row states its own path rationale: "DEC-20260420-H established
direct-SQL as the right path for SA.2b.c — bypasses the onboarding
pipeline's 238-slug manifest/DB drift land mine AND avoids the
--force-executes-live-API-calls behavior that would have made 260
unauthorized paid-provider calls." This sentence names the same subject
matter as the already-resolved (T10 G2 batch 3)
`DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6` record ("Option
C for 238-slug manifest drift... SA.2b.c will use direct-SQL backfill to
avoid the pipeline entirely"), whose own Decision text states the identical
mechanism this row describes. Because the row's own content, not merely the
bare id, matches that specific qualified record's own subject, this record
does not target it as a relation edge (Rule 8's grammar reserves relation
edges for `related_to`/`supersedes`/`amends`/`interprets`/`affirms` types
this batch's method applies collision-to-collision; the cross-collision
match here is noted in prose per this batch's own resolution report rather
than as a graph edge, to avoid asserting a typed relationship the row's own
text does not itself declare). "Prior DECs: DEC-20260420-D (SA.2b PII
classification framework), DEC-20260420-H (direct-SQL path decision)" repeats
the same two references; `DEC-20260420-D` has exactly one formal record
(`DEC-20260420-D--notion-34867c87082c81f0827eedf29d133600`, the duplicate
sibling being documented-only per T10 G2 batch 3), so the relation above
targets it unambiguously.

This historical ID is reused by a second, unrelated Notion row (the
"direct connections only" data-source-type doctrine, a product-scope
decision made the same day) and was an unresolved registry collision until
this batch; the two rows are resolved together in
`archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-I.md`
with distinct source-qualified record keys. This record covers only the
SA.2b.c backfill row.

The historical Notion scope field on this row was `feature`. It is recorded
here as `scope: technical`: it is a data-migration and classification
execution record, not a product or positioning decision.

## Rationale

The rationale, as the row states it: classifying honestly rather than
defaulting all-FALSE removes a silent-misclassification risk for any orphan
slug later revived, at an acknowledged ~10-minute cost; the direct-SQL path
avoids both the 238-slug manifest/DB drift blocker and an unauthorized
live-API-call risk that the ordinary onboarding backfill path would have
triggered.

## Consequences

Status verified on 2026-09-05, against `main`.

**The backfill's end state is confirmed present today.** All 342 manifests
under `manifests/*.yaml` declare a `processes_personal_data` /
`personal_data_categories` field (342 of 342 files match a grep for either
key), consistent with this row's own claim that the DB-side classification
work reached full coverage and with the sibling
`DEC-20260420-J--notion-34867c87082c81478c15cb6985d10137` record's later
claim (same collision-adjacent series) that the NOT NULL flip and heuristic
deletion completed the same week. This record cannot independently verify
the specific `pii_true`/`pii_false` counts the row states (84/191/32) against
current production data, since those are point-in-time database figures,
not manifest content.

**The estonian-company-data fix is live today; the spanish-company-data fix
has since drifted to a different fixture entirely.**
`manifests/estonian-company-data.yaml` carries `registry_code: "17449106"`
and `company_name: Bolt App Services AS` in its known-answer fixture, an
exact match to the row's own text. `manifests/spanish-company-data.yaml`
today has neither a `cif` value nor "Inditex": its `output_schema.example`
uses `company_name: CONSTRUCCIONES AMENABAR SA` and `nif: A20072302`, a
different company and a different field name (`nif`, not `cif`) than the
row's own fix (`cif: 'A15075062'`, Inditex S.A.). This row's fix shipped
and was subsequently superseded by an unrelated later fixture change; no
record in this batch traces that later change.

## Reversal conditions

None recorded on the row (`Superseded By` and `Outcome` are both null). The
row itself frames SA.2b.c as complete and Session A closure (SA.2b.d) as
"next."
