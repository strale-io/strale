---
record_key: DEC-20260905-P
id: DEC-20260905-P
title: Closing-review corrections to the M2 candidate set, round-14 finding and a second resolution report
status: active
topic: m2-closing-review-corrections
scope: operational
owner: claude
decided_at: 2026-09-06
relations:
  - type: amends
    target: DEC-20260905-C
evidence:
  - docs/decisions/records/DEC-20260905-C.md
  - archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-D.md
  - apps/api/src/lib/onboarding-gates.ts
  - archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260505-E.md
  - config/env-manifest.yaml
  - docs/decisions/records/DEC-20260905-G.md
  - archive/sessions/2026-09-05-m2-closing-review-round-14.md
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

The quotation convention for this candidate set is unchanged from
`DEC-20260905-N`: normalize quotation and source before comparing:
transliterate `€` to `EUR`, `×` to `x`, `≥` to `>=`, `≤` to `<=`, `→` to
`->`, `…` to `...`; lowercase; delete every character that is not a
letter or digit. A quotation is faithful when its normalized form is a
substring of the normalized source (an ellipsis splits it into ordered
segments). Punctuation, dashes, quote marks, case, markdown emphasis,
whitespace and the punctuation at a truncation point are therefore never
defects. Defects are: a word inserted, dropped, replaced or reordered; a
composite presented as one quotation; a quotation whose named source does
not contain it (wrong file, function, document, record or row field); a
quotation attributed to a source outside the repository, the parsed row,
the sibling frontend checkout or a merged pull request; and any false
statement about repository state at the commit. `DEC-20260905-N` adds one
clause, unchanged here: a double-quoted span a record attributes to no
source, and that is not presented as the words of a row, file, page or
person, is the record's own wording, judged as prose, not as a quotation.

This record does not withdraw any statement from any decision record.
The two files below are resolution reports, archive files that are never
amended once written (Report Filing Convention). The statements stay in
those files exactly as written; this record notes each by name, the same
treatment `DEC-20260905-G` already gives two other resolution reports
that repeat a statement an amending record withdrew from its formal
record. Once this record lists them, a later closing round treats them
as covered under the round's rule (a), the same as those two.

### `archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-D.md`

1. Notes, as the report has it (lines 91-92): "`apps/api/src/lib/onboarding-gates.ts`
   still enforces `PII_CATEGORY_ENUM` exactly as this row specifies."
   `DEC-20260905-C` item 34 already withdraws this statement from the
   formal record it belongs to,
   `DEC-20260420-D--notion-34867c87082c81f0827eedf29d133600`, naming only
   that record, never this resolution report. Fact, verified against
   `apps/api/src/lib/onboarding-gates.ts:242-260` at this commit:
   `PII_CATEGORY_ENUM` is

   ```
   export const PII_CATEGORY_ENUM = [
     "name",
     "email",
     "phone",
     "address",
     "date_of_birth",
     "government_id",
     "financial",
     "professional",
     "behavioral",
     "biometric",
     "health",
     "sensitive_special",
     // Added 2026-04-30 to cover sanctions/PEP screening manifests.
     // `nationality` is country-of-citizenship; `political_affiliation`
     // is an Art. 9 special-category attribute for PEP records.
     "nationality",
     "political_affiliation",
   ] as const;
   ```

   fourteen entries: the twelve the row named, plus `nationality` and
   `political_affiliation`, added per the inline comment dated
   2026-04-30 quoted above. The enum is not "exactly as this row
   specifies" and has not been since that addition. This report is an
   immutable archive file and is not edited to fix the sentence; it is
   noted here, not corrected, so a future round treats it as covered.


### `archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260505-E.md`

2. Notes, as the report has it (line 45): "`config/env-manifest.yaml`
   still carries eight `HMRC_*` credential rows". `DEC-20260905-N` item 2
   already withdraws the "eight" count from the formal record it belongs
   to, `DEC-20260505-E--notion-35767c87082c813481a8efa27ea37438`, naming
   only that record, never this resolution report. Fact, verified against
   `config/env-manifest.yaml` at this commit: the file carries seven
   `HMRC_*` rows, `HMRC_CLIENT_ID`, `HMRC_CLIENT_SECRET`,
   `HMRC_REQUESTER_VRN`, `HMRC_SANDBOX_CLIENT_ID`,
   `HMRC_SANDBOX_CLIENT_SECRET`, `HMRC_TEST_VRN` and `HMRC_USE_SANDBOX`,
   the same seven the formal record's own sentence enumerates. This
   report is an immutable archive file and is not edited to fix the
   sentence; it is noted here, not corrected, so a future round treats it
   as covered.

## Context

Round 14 of the closing review ran at commit
`d0e21ecdb3009c8ce83a5345c95755c8cc386ec1` with six read-only partition
reviewers (P1 through P6) and a gate run. All nine gates exited 0 and
all six partitions returned `PARTITION VERDICT: PASS` with no numbered
findings; partition P5 separately flagged the statement above as an
"ancillary observation, not a finding against a graded record," since
the resolution report lies outside the 34 record files P5 was assigned
to review. The consolidator's verdict ruled the statement a stated
limitation and wrote a PASS verdict on a pull request; the independent
review of that verdict pull request ruled that an unlisted stale
resolution-report statement is a finding under the round's rule (a),
because that rule's exemption names only resolution-report statements a
listed amending record lists, and this one was not listed by any
`DEC-20260905-*` record until now. The orchestrator accepted that
ruling; the verdict pull request (#611) was closed unmerged. The six
partition reports and the gate output from this round are archived by
this PR at
`archive/sessions/2026-09-05-m2-closing-review-round-14.md`.

Item 2 was not found by round 14. The independent review of this
record's own pull request swept the resolution reports for the same
class and found a second report asserting a statement an amending record
had withdrawn, in wording the orchestrator's earlier sweep did not match
because the report's phrasing differs from the formal record's. It is
noted here rather than left for a later round.

## Rationale

An exemption that names specific artifacts is a closed list and is not
widened by analogy at the moment it would excuse a false statement;
noting the report by name is the treatment the corpus already uses for
this class, and it costs nothing to apply it here.

## Consequences

(a) Round 15 treats a statement withdrawn by `DEC-20260905-B` through
`-O`, or noted by this record, as corrected.

(b) The amended record (`DEC-20260905-C`) keeps its text, as does
`DEC-20260905-N`, whose item 2 the second note extends; the corrections
live only here, and neither resolution report is ever edited.

(c) **Not adopted**: none. Both items are notes verified against the
files at this commit; nothing this record's own review found is left
uncorrected and unnoted.

**Relation to `DEC-20260905-C`**: extends item 34's withdrawal, which
names only the formal record `DEC-20260420-D--notion-34867c87082c81f0827eedf29d133600`,
to the resolution report that repeats the same withdrawn statement in
its own voice; item 34 itself is undisturbed, and so is every other item
in `DEC-20260905-C`. Item 2 above stands in the same relation to
`DEC-20260905-N` item 2, which withdrew the "eight `HMRC_*` rows" count
from its formal record and named only that record; this record adds no
`amends` relation to `DEC-20260905-N`, because it withdraws nothing from
it and leaves that item undisturbed.

## Reversal conditions

A later closing-review round that shows the correction in this record to
be wrong produces a further amending record, never an edit to this one.
If a reviewed in-place correction mechanism for candidate records is
adopted before the M4 cutover, that mechanism may supersede this record.
The M4 cutover itself does not reverse this record.
