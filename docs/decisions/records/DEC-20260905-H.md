---
record_key: DEC-20260905-H
id: DEC-20260905-H
title: Closing-review corrections to the M2 candidate set, round 7
status: active
topic: m2-closing-review-corrections
scope: operational
owner: claude
decided_at: 2026-09-06
relations:
  - type: amends
    target: DEC-20260420-H--notion-34867c87082c81b58b36de5f71c0937f
  - type: amends
    target: DEC-20260430-A
  - type: amends
    target: DEC-20260506-G
evidence:
  - archive/sessions/2026-09-05-m2-closing-review-round-7.md
  - docs/decisions/records/DEC-20260420-H--notion-34867c87082c81b58b36de5f71c0937f.md
  - docs/strategy/2026-08-05-direction-plan.md
  - CLAUDE.md
  - docs/decisions/records/DEC-20260430-A.md
  - docs/decisions/records/DEC-20260428-A.md
  - docs/decisions/records/DEC-20260428-B.md
  - https://app.notion.com/35267c87082c81eca01cf6eedb5eafeb
  - docs/decisions/records/DEC-20260506-G.md
  - docs/decisions/records/DEC-20260422-H.md
  - docs/decisions/id-collisions.yaml
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

The quotation convention for this candidate set is unchanged from
`DEC-20260905-G`: normalize quotation and source before comparing:
transliterate `€` to `EUR`, `×` to `x`, `≥` to `>=`, `≤` to `<=`, `→` to
`->`, `…` to `...`; lowercase; delete every character that is not a
letter or digit. A quotation is faithful when its normalized form is a
substring of the normalized source (an ellipsis splits it into ordered
segments). Punctuation, dashes, quote marks, case, markdown emphasis,
whitespace and the punctuation at a truncation point are therefore never
defects. Defects are: a word inserted, dropped, replaced or reordered; a
composite presented as one quotation; a quotation whose named source does
not contain it (wrong file, function, document, record or row field); a
quotation attributed to a source outside the repository, the parsed row,
the sibling frontend checkout or a merged pull request; and any false
statement about repository state at the commit. The relation rule is also
unchanged: a relation declared in a record's frontmatter whose basis is
stated in an amending record, or narrated in the target record's own
body, is substantiated; the narration need not live in the source
record's own body once that basis is stated somewhere reachable from the
record.

The statements listed below are withdrawn from the named records and
replaced by the facts stated here. Two further items substantiate,
without withdrawing anything, a relation this round's review confirmed
holds but whose basis had not previously been stated as a fresh item
against this specific pair of targets.

### `DEC-20260420-H--notion-34867c87082c81b58b36de5f71c0937f`

1. Withdraws, as the record has it (Consequences section): "`docs/strategy/2026-08-05-direction-plan.md`
   states Part One (\"library-as-product\") is the adopted operating
   strategy per `DEC-20260812-A` (existing record)...". Fact: at this
   commit, `docs/strategy/2026-08-05-direction-plan.md` does not contain
   the compound phrase "library-as-product" anywhere; its own wording at
   the relevant points reads "This plan commits to the library as the
   product" (line 14) and the section heading "Part One — The library,
   built properly" (line 64). The phrase "library-as-product" does exist
   verbatim in this repository, but in `CLAUDE.md:302`'s own summary
   bullet for `DEC-20260812-A` ("The 2026-08-05 Direction Plan Part One
   (library-as-product, x402 primary rail)..."), not in the direction-plan
   document the record names as the source of the quotation. This is a
   quotation attributed to the wrong source document: `CLAUDE.md`'s own
   gloss on the direction plan, presented as the direction plan's own
   wording. The underlying substantive point this record makes (a later,
   dated strategy document supersedes whatever positioning this row
   adopted, per `DEC-20260812-A`) is not disturbed; only the attribution
   of the specific quoted phrase to the wrong file is corrected. Evidence:
   `docs/strategy/2026-08-05-direction-plan.md` (lines 14, 64); `CLAUDE.md`
   (line 302).

### `DEC-20260430-A`

2. Restates the substantiation DEC-20260905-F item 1 already records, so that both amending records carry it: `DEC-20260430-A`'s frontmatter declares a
   `related_to` relation to `DEC-20260428-A`. `DEC-20260430-A`'s own
   Context section states, without naming the target by ID: "It explicitly
   kept the third-party sourcing doctrine and the engineering bar as
   governing context." `DEC-20260428-A`'s own frontmatter `title` reads
   "Adopt a three-tier doctrine for third-party scraping" and its `topic`
   is `third-party-scraping-doctrine`; no other record in this corpus
   adopts a three-tier scraping doctrine. This is the relation's basis:
   the phrase "the third-party sourcing doctrine" in `DEC-20260430-A`'s
   own Context section unambiguously identifies `DEC-20260428-A` by its
   unique subject matter, even though the ID is not spelled out, and
   `DEC-20260430-A` (decided 2026-04-30) explicitly says it kept that
   doctrine as governing context for the vendor-stack canonicalization the
   record performs. `DEC-20260430-A`'s own Notion row (page
   `35267c87082c81eca01cf6eedb5eafeb`) has a null `Rationale` field and
   supplies no additional basis beyond what the record's own Context
   section already states. This item independently re-derives the same
   basis `DEC-20260905-F` item 1 already established; it is recorded here,
   against this round's own review, because round 7's brief asked this
   relation to be re-substantiated as a fresh item rather than referenced
   only by pointer. Evidence: `docs/decisions/records/DEC-20260430-A.md`
   (Context section); `docs/decisions/records/DEC-20260428-A.md`
   (frontmatter `title` and `topic`).
3. Restates the substantiation DEC-20260905-F item 2 already records, so that both amending records carry it: `DEC-20260430-A`'s frontmatter declares a
   second `related_to` relation, to `DEC-20260428-B`, with the identical
   pattern: not narrated by ID in the record's own body. The same Context
   sentence identified in item 2 also names the second target by subject
   matter: "...and the engineering bar as governing context." `DEC-20260428-B`'s
   own frontmatter `title` reads "Set the engineering bar for
   Strale-built regulatory data services" and its `topic` is
   `regulatory-data-service-engineering-bar`; no other record in this
   corpus sets this engineering bar. This is the relation's basis: "the
   engineering bar" in `DEC-20260430-A`'s own Context section
   unambiguously identifies `DEC-20260428-B` by its unique subject matter,
   and `DEC-20260430-A` names it, alongside the sourcing doctrine, as
   governing context it explicitly kept. This item independently
   re-derives the same basis `DEC-20260905-F` item 2 already established,
   for the same reason given in item 2 above. Evidence:
   `docs/decisions/records/DEC-20260430-A.md` (Context section);
   `docs/decisions/records/DEC-20260428-B.md` (frontmatter `title` and
   `topic`).

### `DEC-20260506-G`

4. Withdraws, as the record has it (Context section): "a pricing benchmark
   study (`DEC-20260422-H`, no formal record exists for that id on `main`
   and it is not in `docs/decisions/id-collisions.yaml`, mentioned in
   prose only)". Fact: `docs/decisions/records/DEC-20260422-H.md` exists
   in this repository at this commit (it is itself one of this same
   batch's own reviewed records, "Defer Payee Assurance v1 pricing
   commitment..."). The "no formal record exists for that id" half of the
   sentence is false as literally stated; the companion "not in
   `docs/decisions/id-collisions.yaml`" half is true and is not disturbed.
   The record's immediately following clause, about `DEC-20260506-F`
   ("no formal record exists for that id either, mentioned in prose
   only"), is true: no `DEC-20260506-F.md` file exists in this repository
   and it is not in `docs/decisions/id-collisions.yaml`; that clause is
   not withdrawn. This is the same class of defect `DEC-20260905-D` and
   `-E` withdrew for `DEC-20260405-A`/`DEC-20260409-D`'s false "no record
   exists" claims about other ids, but this specific instance, on this
   specific record and target id, was not named by any of
   `DEC-20260905-B` through `-G` and is a fresh, unwithdrawn finding this
   round. Evidence: `docs/decisions/records/DEC-20260422-H.md` (exists);
   `docs/decisions/id-collisions.yaml` (no `DEC-20260422-H` or
   `DEC-20260506-F` entry).

## Context

Round 7 (final round) of the M2 closing independent review ran at commit
`f15bbdd9e7cb88401771cedb62c5907636bf7477`, after `DEC-20260905-G`
withdrew round 6's confirmed statements and its corpus-wide sibling-state
sweep finding. Six fresh, read-only reviewers applied the quotation
convention `DEC-20260905-D`/`-E`/`-F`/`-G` state, in six partitions
covering the full 237-record corpus. P1, P2, P4, and P6 passed with no
confirmed defects; P3 found one confirmed defect (the false "no formal
record exists" claim withdrawn in item 4 above; P3 also separately raised
`DEC-20260429-A`'s "four review triggers" enumeration, addressed below
under Not adopted, not as a withdrawal); P5 found one confirmed defect
(the misattributed quotation withdrawn in item 1 above). All nine gates
ran clean at this commit (exit 0 each; `npm run context:check`, `npm run
context:test`, `node --test scripts/decision-records.test.mjs
scripts/m2-closure-register.test.mjs`, `node
scripts/m2-closure-verify-private-rows.mjs`, `npm run programs:check`,
`npm run codex:check`, `npm run receipts:check`, `node
apps/api/scripts/check-pii.mjs --strict`, `node
apps/api/scripts/check-no-committed-secrets.mjs`); the run is valid.

This record's own reconciliation additionally ran the operator checker
over the full corpus (237 records, 1147 spans, 1048 faithful, **99**
residual) at this same commit and read every residual directly against
its named source rather than trusting the checker's best-match guess.
Full reconciliation, entry by entry:
`scratchpad/residual-reconciliation-round7.md` (not committed; this
summary is the citable form). Of the 99: **0** were already withdrawn by
`DEC-20260905-B` through `-G`; **0** are new defects; **99** were checker
misses, each faithful to a located and quoted source (11 individually
verified across 11 files, all corroborated by this round's own P2, P3,
and P4 partition reports; 88 self-referential parsing artifacts inside
`DEC-20260905-C.md` (82), `-D.md` (1), and `-F.md` (5), the same class
rounds 3 through 6's reconciliations described and quantified for the
immutable already-merged withdrawal records). A second checker run at
`--min-chars 12` (122 residual, 23 beyond the default run's 99)
reconciles the same way, entry by entry, in
`scratchpad/residual-reconciliation-round7-short.md` (not committed):
**0** already withdrawn; **0** new defects; **23** checker misses, each
faithful to a located source (short, generic, self-referential, or
paraphrased spans the checker's fuzzy matcher could not place). The
19-character `"library-as-product"` misattribution withdrawn in item 1
above is not among either run's residuals at any threshold tested; it was
found only by reading the record and its cited source directly, the same
class `DEC-20260905-F` documented for `DEC-20260505-H`'s 22-character
misattribution.

Beyond the six partitions' own coverage, this record additionally ran the
broader sibling-state re-sweep this round's brief required, using a wider
grep pattern than round 6's own sweep (adding, among others, `not (a|yet)
(a )?formal`, `has no (formal )?record`, `without a (formal )?record`,
`no candidate record`, `pending (migration|record)`, and `record for`
alongside round 6's fourteen patterns), over `docs/decisions/records/*.md`
excluding the six `DEC-20260905-B` through `-G` amending records
themselves. 56 raw hits, each read in context and checked against
`docs/decisions/records/` and `docs/decisions/id-collisions.yaml` at this
commit; full sweep: `scratchpad/sibling-state-sweep-round7.md` (not
committed). It found exactly one statement not already withdrawn by a
prior round and not still true: item 4 above, `DEC-20260506-G`'s stale
claim about `DEC-20260422-H`, independently corroborating P3's own
direct-reading finding rather than surfacing a second, distinct instance
the six partitions' own contiguous coverage missed. The round-6 sibling-state
sweep listed `DEC-20260506-G` among its raw hits but did not withdraw its
false half; this record closes that gap.

All reviewed records are `status: active`.
`validateProtectedDecisionChange` in `scripts/decision-records-lib.mjs`
refuses any commit that changes an active record's protected sections
(`DECISION_ACTIVE_BODY_CHANGED`) or its metadata
(`DECISION_ACTIVE_METADATA_CHANGED`). Correcting a false or misattributed
statement inside an active record therefore cannot go through an edit to
that record; it goes through a new record (this one) that names the
withdrawal, the same mechanism `DEC-20260905-B` through `-G` used for
rounds 1 through 6.

## Rationale

One amending record per remediation round keeps history immutable, keeps
the reviewer trail intact, keeps the validator's protections untouched,
and matches a stable evidence file
(`archive/sessions/2026-09-05-m2-closing-review-round-7.md`). The
quotation convention, restated unchanged from `DEC-20260905-G`, continues
to remove reviewer disagreement about what counts as a defect; it does
not weaken the bar on words, attribution, or facts. A checker residual is
a defect unless the source text that contains it is located and quoted;
this record's own reconciliation quotes or names the located source for
every one of the 99 default-run and 23 additional short-run checker
misses it reports.

A sweep is only as complete as its patterns, so this record's re-sweep
lists every hit of a broader pattern with an explicit verdict rather than
only the ones judged relevant, the same discipline round 6's own sweep
established. Round 6's sweep already listed `DEC-20260506-G` among its raw
hits but stopped at "the sweep found exactly one further instance" without
separately confirming whether every other hit's individual clause was
true or false; this record's re-sweep judges the 56 hits it finds
individually, one line per hit, so a partially-true, partially-false
sentence like `DEC-20260506-G`'s (true about `DEC-20260506-F`, false about
`DEC-20260422-H`) cannot be marked "already covered" on the strength of
the true half.

## Consequences

(a) Round 8 treats a statement withdrawn here, in `DEC-20260905-B`,
`DEC-20260905-C`, `DEC-20260905-D`, `DEC-20260905-E`, `DEC-20260905-F`, or
`DEC-20260905-G` as corrected, and a relation substantiated in any of
those records or in items 2-3 above as substantiated, and applies the
quotation convention stated above.

(b) Amended records keep their text; the correction lives only here.

(c) **Not adopted**: findings, observations, and items this record
deliberately did not treat as a withdrawal or substantiation, with
reasons:

- **`DEC-20260429-A`'s "four review triggers" enumeration**, raised by
  P3's round-7 report after checking the row properties and a handoff
  file. The Decisions-database page body of `DEC-20260429-A` (page
  `35167c87082c8172bff8f3485699c961`, section "Re-evaluation triggers")
  lists exactly those four triggers; `DEC-20260905-E`'s Consequences
  already record this as verified from the page body. Recorded here again
  under Not adopted, citing `DEC-20260905-E`, not as a withdrawal.
- **Any round-7 report's finding that a claim about database or
  production state is unverifiable.** Every such claim stays as the
  record itself states it; none is withdrawn on the strength of a
  read-only repository review.
- **Convention-covered classes among the 99 default-run and 23 short-run
  checker residuals** (a record's own grep/search pattern presented as a
  quotation of its methodology rather than a source's content; a record's
  own rhetorical paraphrase or illustrative phrasing not attributed to
  any source; a self-quotation of the same record's own earlier text; a
  quotation verified faithful to a file not listed in the record's own
  `evidence:` array but named in its prose; the 88 self-referential
  parsing artifacts inside `DEC-20260905-C.md`, `-D.md`, and `-F.md`).
  These are checker misses this record's reconciliation locates a source
  for (`scratchpad/residual-reconciliation-round7.md`,
  `scratchpad/residual-reconciliation-round7-short.md`); none is a
  withdrawal target.
- **Every other line in the broader sibling-state re-sweep**
  (`scratchpad/sibling-state-sweep-round7.md`): 55 of the 56 raw hits are
  true, not a state claim about another record's or collision's
  existence, or already withdrawn by a named item in `DEC-20260905-C`,
  `-D`, `-E`, or `-G`. None of these is a fresh withdrawal target.

(d) The operator checker `scripts/m2-quote-fidelity.mjs`'s residual list
at commit `f15bbdd9e7cb88401771cedb62c5907636bf7477`
(`scratchpad/fidelity-residuals-round7.json`, 99 residual spans at the
default 25-character threshold) reconciles as: **0** already withdrawn by
`DEC-20260905-B` through `-G`; **0** new defects; **99** checker misses,
each faithful to a located source (11 individually verified across 11
files; 88 self-referential artifacts inside `DEC-20260905-C.md` (82),
`-D.md` (1), `-F.md` (5)). A second run at `--min-chars 12`
(`scratchpad/fidelity-residuals-round7-short.json`, 122 residual spans,
23 beyond the default run) reconciles as: **0** already withdrawn; **0**
new defects; **23** checker misses, each faithful to a located source.
Full reconciliation, entry by entry:
`scratchpad/residual-reconciliation-round7.md` and
`scratchpad/residual-reconciliation-round7-short.md` (not committed;
this summary is the citable form).

**Relation to `DEC-20260420-H--notion-34867c87082c81b58b36de5f71c0937f`**:
withdraws the misattributed `"library-as-product"` quotation in item 1
above.

**Relation to `DEC-20260430-A`**: substantiates, without withdrawing
anything, its two undeclared-by-name `related_to` relations to
`DEC-20260428-A` and `DEC-20260428-B` (items 2-3 above).

**Relation to `DEC-20260506-G`**: withdraws the false "no formal record
exists" characterization of `DEC-20260422-H` in item 4 above.

## Reversal conditions

A later closing-review round that shows a correction in this record to be
wrong produces a further amending record, never an edit to this one. If a
reviewed in-place correction mechanism for candidate records is adopted
before the M4 cutover, that mechanism may supersede this record. The M4
cutover itself does not reverse this record.
