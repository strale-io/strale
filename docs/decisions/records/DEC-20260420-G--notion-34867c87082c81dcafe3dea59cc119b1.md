---
record_key: DEC-20260420-G--notion-34867c87082c81dcafe3dea59cc119b1
id: DEC-20260420-G
title: "Entity resolution as priority engineering investment"
status: active
topic: entity-resolution-priority-investment
scope: product
owner: petter
decided_at: 2026-04-20
relations: []
evidence:
  - https://app.notion.com/34867c87082c81dcafe3dea59cc119b1
  - docs/decisions/records/DEC-20260409-B.md
  - apps/api/src/lib/company-name-match.ts
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

The row's Notion title, "Entity resolution as priority engineering
investment," is the only content the export preserves. Its `Rationale` and
`Outcome` fields are both null; no further description of what investment
or scope was chosen is available from this export.

## Context

This row carries a title only. No Rationale, no Outcome, and no `Source`
link were exported for it. What follows is inferred from the row's
creation date (2026-04-20) and title alone, stated explicitly as inference,
not as the row's own words.

This historical ID is reused by a second, unrelated Notion row (F-A-012
verify endpoint DoS hardening, a technical implementation decision made the
same day) and was an unresolved registry collision until this batch; the
two rows are resolved together in
`archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-G.md`
with distinct source-qualified record keys. This record covers only the
"Entity resolution as priority engineering investment" row.

The historical Notion scope field on this row was `global`. It is recorded
here as `scope: product`: its title names an investment-priority subject,
not an implementation.

## Rationale

Not recorded on the row. No rationale text was exported.

## Consequences

Status verified on 2026-09-05, against `main`.

**A concrete entity-resolution investment exists in the codebase, but a
prior batch already found half of it dead code, not a full delivery of
this row's "priority" framing.** `apps/api/src/lib/company-name-match.ts`
exists as a fuzzy company-name matching module.
`docs/decisions/records/DEC-20260409-B.md` (existing record, "Code-based
lookup pattern + cross-validation for entity resolution," decided two
weeks before this row) states in its own Consequences: "Part 2, the
cross-validation layer, was built as a standalone module but... file is
itself orphaned: no capability executor under [wired into the solution
executor]; the cross-validation half is dead code." This row's title
implies a later, larger prioritization of engineering effort toward entity
resolution than `DEC-20260409-B`'s own scope, but its null Rationale gives
no specifics against which to verify whether that larger investment
actually followed. The one component this record can point to
(`company-name-match.ts`) predates this row by design (it is the code
`DEC-20260409-B` names), so it cannot itself be evidence of this row's
"priority" claim.

## Reversal conditions

None recorded on the row (`Superseded By` and `Outcome` are both null). If
this row's Notion body ever becomes available, its specific investment
claims should be verified against `DEC-20260409-B`'s already-documented
orphaned cross-validation module rather than left as inference.
