---
record_key: DEC-20260420-F--notion-34867c87082c810b8df1e8e459039d35
id: DEC-20260420-F
title: "Capability rationalization and site rebuild"
status: active
topic: capability-rationalization-site-rebuild
scope: product
owner: petter
decided_at: 2026-04-20
relations: []
evidence:
  - https://app.notion.com/34867c87082c810b8df1e8e459039d35
  - docs/decisions/records/DEC-20260513-A.md
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

The row's Notion title, "Capability rationalization and site rebuild," is
the only content the export preserves. Its `Rationale` and `Outcome` fields
are both null; no further description of what rationalization or rebuild
was chosen is available from this export.

## Context

This row carries a title only. No Rationale, no Outcome, and no `Source`
link were exported for it. What follows is inferred from the row's
creation date (2026-04-20) and title alone, stated explicitly as inference,
not as the row's own words.

This historical ID is reused by a second, unrelated Notion row (F-A-006 +
F-A-007 HMAC audit token lifecycle, a technical implementation decision
made the same day) and was an unresolved registry collision until this
batch; the two rows are resolved together in
`archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-F.md`
with distinct source-qualified record keys. This record covers only the
"Capability rationalization and site rebuild" row.

The historical Notion scope field on this row was `global`. It is recorded
here as `scope: product`: its title names a product/site-architecture
subject, not an implementation.

## Rationale

Not recorded on the row. No rationale text was exported.

## Consequences

Status verified on 2026-09-05, against `main`.

**"Site rebuild" has not happened as this row's title names it, and the
current, later governing decision commits to a different mechanism.**
`apps/` in this repository contains only `api` (`apps/api`); no `apps/web`
directory exists as of this batch. `DEC-20260902-A` (CLAUDE.md's Current
Decisions, September 2026 section) states: "The website redesign is built
inside this repository as `apps/web` (monorepo)... `strale-frontend` was
swept and its design material preserved... and is kept, not extended,
until the `apps/web` site serves production." That decision post-dates
this row by over four months and describes a website rebuild still not
begun in this repository, not a completion of whatever rebuild this row
named. `docs/decisions/records/DEC-20260513-A.md` (existing record, title:
"Hosting plan in DEC-20260503-C partially superseded: strale-frontend on
Cloudflare Pages (not Railway as planned); sibling-repo structure retained
(monorepo deferred); payment rails portion of DEC-20260503-C remains
active") is the nearest dated record on the hosting/repo-structure
question this row's "site rebuild" would have touched, and it explicitly
deferred the monorepo structure DEC-20260902-A later adopted. This record
does not claim DEC-20260513-A is this row's direct successor; it is cited
as the nearest evidence of how the repo/site question evolved between this
row and DEC-20260902-A.

## Reversal conditions

None recorded on the row (`Superseded By` and `Outcome` are both null). If
this row's Notion body ever becomes available, its "capability
rationalization" and "site rebuild" content should be reconciled against
`DEC-20260513-A` and `DEC-20260902-A` directly rather than left as
inference.
