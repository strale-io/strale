---
record_key: DEC-20260315-B
id: DEC-20260315-B
title: "Code pattern publishing starts Week 1"
status: active
topic: code-pattern-publishing-week-1
scope: product
owner: petter
decided_at: 2026-03-15
relations: []
evidence:
  - https://app.notion.com/32367c87082c8119be47c14271503995
  - context7.json
  - docs/ide-rules/strale-compliance.mdc
  - docs/ide-rules/strale-compliance.windsurfrules
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

Elevate canonical code pattern publishing from Phase C (Week 3-4) to
Week 1: the first 2-3 GitHub gists ship at Launch +2-3d.

## Context

The row's stated basis, from its own Rationale: "DEC-20260311-A
(canonical code patterns) originally deferred to Phase C (Week 3-4).
Elevated to Week 1, first 2-3 GitHub gists ship Launch +2-3d. Every day
without public `strale.do()` examples is a day models aren't learning to
recommend Strale. Supabase playbook proves models recommend tools
they've been trained on." The row names DEC-20260311-A as the decision
it is elevating; that ID has no formal record in this repository
(`test -f docs/decisions/records/DEC-20260311-A.md` fails, and it is not
in this batch or an earlier one), so no relation edge is recorded here,
only this prose note.

The historical Notion scope field on this row was `global`. It is
recorded here as `scope: product`: it is a distribution-timing decision
about developer-facing content, not a company-wide strategic or
positioning decision on its own.

## Rationale

The rationale, as the row states it: LLMs recommend tools they were
trained on seeing used in public code, so every day without public
`strale.do()` examples is lost training-data exposure; the Supabase
playbook is cited as a precedent for this effect.

## Consequences

Status verified on 2026-09-05, against `main`.

**No GitHub gists exist as the row describes them, but a different code-pattern
distribution mechanism has since been built.** This repository carries
`context7.json`, which registers `docs`, and every packages/*-strale SDK
and framework-plugin directory with the Context7 documentation index
(`"folders": ["docs", "packages/sdk-typescript", "packages/sdk-python",
"packages/mcp-server", "packages/langchain-strale", "packages/crewai-strale",
"packages/semantic-kernel-strale", "packages/openai-agents-strale",
"packages/pydantic-ai-strale", "packages/google-adk-strale"]}`), and
`docs/ide-rules/` carries IDE-rule files
(`strale-compliance.mdc`, `strale-compliance.windsurfrules`). A later
formal record, DEC-20260330-B (T10 batch, existing on `main`), titles
itself "Shift distribution from 'be listed' to 'be embedded in coding
workflow' via Context7, IDE rules, vibe-coding SEO" and cites exactly
these files as evidence. This record does not create a relation edge to
DEC-20260330-B: this row's own text does not name it (it predates that
decision by 16 days), so the connection is noted here as context, not as
a source-stated relationship. No standalone GitHub-gist artifact search
was possible from inside this repository (gists are not repo-tracked
content), so whether the specific 2-3 gists this row describes shipped
cannot be confirmed or denied from the code alone.

## Reversal conditions

None recorded on the row (`Superseded By` and `Outcome` are both null).
DEC-20260330-B's Context7/IDE-rules/SEO strategy is the closest surviving
artifact to what this row describes, though it is a distinct, later
decision rather than a direct continuation on the record.
