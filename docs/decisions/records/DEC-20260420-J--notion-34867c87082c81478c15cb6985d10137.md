---
record_key: DEC-20260420-J--notion-34867c87082c81478c15cb6985d10137
id: DEC-20260420-J
title: 'SA.2b.d closes Session A: 32 orphans classified (15 TRUE/17 FALSE), processes_personal_data flipped to NOT NULL with default false, heuristic fallback deleted from audit-helpers.ts + all three call sites in do.ts. Session A formally complete.'
status: active
topic: sa2bd-session-a-closure
scope: technical
owner: petter
decided_at: 2026-04-20
relations:
  - type: related_to
    target: DEC-20260420-D--notion-34867c87082c81f0827eedf29d133600
  - type: related_to
    target: DEC-20260420-E--notion-34867c87082c81b590b4e8bee4b59228
  - type: related_to
    target: DEC-20260420-F--notion-34867c87082c810b8547fccb3e75c61b
  - type: related_to
    target: DEC-20260420-G--notion-34867c87082c81c38c3acaca5d01d6ef
  - type: related_to
    target: DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6
evidence:
  - https://app.notion.com/34867c87082c81478c15cb6985d10137
  - apps/api/src/lib/audit-helpers.ts
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

Four phases in one session: (1) orphan classification of 32 rows via direct
SQL pattern-matching (15 TRUE, 17 FALSE); (2) migration 0050 flips
`processes_personal_data` to `NOT NULL DEFAULT false` (commit `cf33028`);
(3) deletes the `detectPersonalData` heuristic from `audit-helpers.ts` and
all three call sites in `do.ts` (commit `6dfb47f`); (4) deploy plus three
production spot-checks confirming the DB-to-runtime path reads directly,
with no heuristic fallback remaining. The row states this closes Session A
formally: "Strale's compliance infrastructure has no outstanding Session A
findings."

## Context

The row's own reasoning for real orphan classification over an all-FALSE
default: "some orphans may be revived in Session B; silent
misclassification if resurrected without re-classification is the bad
failure mode." It lists a full Session A findings closure table by F-A
number: "F-A-001: shipped (DEC-20260420-C)... F-A-003: shipped today
(SA.2b + SA.2b.d)... F-A-005: shipped (DEC-20260420-E)... F-A-006 + F-A-007:
shipped (DEC-20260420-F)... F-A-009: shipped today... F-A-011: deferred to
SCF-2... F-A-012: shipped (DEC-20260420-G)."

`DEC-20260420-C` is referenced twice (F-A-001, F-A-004) but has no formal
record or collision-registry entry in this repository at the time of this
batch; it is recorded here as an unresolved reference, not a relation edge.
`DEC-20260420-E` and `DEC-20260420-F` are each historical IDs reused by two
Notion rows (T10 G2 batch 3); the row's own "F-A-005" and "F-A-006 +
F-A-007" labels are exact title matches to
`DEC-20260420-E--notion-34867c87082c81b590b4e8bee4b59228` ("F-A-005
free-tier transaction lookup redaction...") and
`DEC-20260420-F--notion-34867c87082c810b8547fccb3e75c61b` ("F-A-006 +
F-A-007 HMAC audit token lifecycle...") respectively, so the relations
above target those two specific qualified records rather than their
unrelated siblings ("Product architecture and first wedge" and
"Capability rationalization and site rebuild"). `DEC-20260420-G`'s
"F-A-012" label is likewise an exact title match to
`DEC-20260420-G--notion-34867c87082c81c38c3acaca5d01d6ef` ("F-A-012 verify
endpoint DoS hardening..."), not its "Entity resolution" sibling.
`DEC-20260420-D` is a single-formal-record collision
(`DEC-20260420-D--notion-34867c87082c81f0827eedf29d133600`), targeted
directly for the row's own "DEC-20260420-D OQ #6 (heuristic retention):
closed per commitment" reference.

The row separately states: "Manifest/DB drift cleanup (238 slugs Class 4):
deferred to Session B per DEC-20260420-H." This names the exact subject
matter of `DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6`
("Option C for 238-slug manifest drift... defer full Class 4 cleanup to
Session 1 onboarding engine rewrite"), so that relation targets the
manifest-drift record specifically, not its "Strale positioning and ICP
clarification" sibling.

The row closes with "Prior Session A DECs: DEC-20260420-A through
DEC-20260420-I (complete remediation chain)." **This is a range expansion,
recorded here as inference, not as the row's literal wording.** Of the
nine letters in that range: `DEC-20260420-A` exists as a single,
non-collision record (`docs/decisions/records/DEC-20260420-A.md`);
`DEC-20260420-B` and `DEC-20260420-C` have no record or collision-registry
entry in this repository and are not targetable; `DEC-20260420-D` through
`DEC-20260420-H` are covered by the specific relations above (this row's
own F-A/manifest-drift citations already identify one qualified record per
letter where one is identifiable); `DEC-20260420-I` is this same batch's
other collision, covering both its rows (the doctrine row and the SA.2b.c
backfill row); this record does not add a further edge to either, since
the range statement does not by itself distinguish which of the two rows
it means, and the row's own SA.2b.c-adjacent subject matter is documented
in that record's own resolution report rather than asserted here as a typed
edge. No relation edge targets `DEC-20260420-A` itself in this record's
frontmatter: the range citation names it only as part of a nine-letter
span, with no more specific content connecting this row to it beyond the
inference stated here.

This historical ID is reused by a second Notion row, historically
superseded (the original bank-verification-deferral decision, replaced
same-day by `DEC-20260420-K`), and was an unresolved registry collision
until this batch; the two rows are resolved together in
`archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-J.md`,
with the superseded row documented-only rather than a second formal record.
This record covers only the SA.2b.d closure row.

The historical Notion scope field on this row was `feature`. It is recorded
here as `scope: technical`: it is a migration-and-deletion execution record.

## Rationale

The rationale, as the row states it: real classification over an all-FALSE
default removes silent-misclassification risk for revivable orphans at
acceptable cost; the DEFAULT false on the NOT NULL column is "belt-and-
suspenders" against any future onboarding path that bypasses the manifest
gate. The row's own closure table marks the heuristic deletion as closing
"F-A-003 (input-PII missed by heuristic): closed. Heuristic deleted;
manifest-declared values are authoritative." The phrase "Exact same
anti-pattern as F-A-003" describing this deletion belongs to the sibling
`DEC-20260420-K--notion-34867c87082c8198b6ecf3569a68a9b4` row's own OQ-6
text, not this row's.

## Consequences

Status verified on 2026-09-05, against `main`.

**The end state (NOT NULL, no heuristic) matches production manifests
today.** All 342 manifests under `manifests/*.yaml` declare
`processes_personal_data` or `personal_data_categories`; this record did not
re-run the row's own production spot-check queries (`pep-check`,
`dns-lookup`, `currency-convert` transaction ids) and so cannot confirm the
specific transaction-level values the row cites, only that the schema-level
end state (a non-null, manifest-declared field, no default heuristic
fallback active on 342/342 manifests) is consistent with the row's own
described outcome.

**Three of the row's five outstanding follow-ups are unverifiable from
this evidence set** (Python SDK sync, HMAC rotation staging drill,
`phone-validate` SQS=0 investigation reference the deleted SQS engine and
external to-do pages, not files in this repository). The manifest/DB drift
cleanup this row deferred to Session B is addressed by
`DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6`'s own
Consequences section (T10 G2 batch 3): resolved by a direct-SQL backfill
path rather than the Session-1 onboarding-engine rewrite this row's sibling
deferral named.

## Reversal conditions

None recorded on the row (`Superseded By` and `Outcome` are both null). The
row states Session A is "formally complete," with no reopening condition of
its own.
