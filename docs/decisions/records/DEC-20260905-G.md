---
record_key: DEC-20260905-G
id: DEC-20260905-G
title: Closing-review corrections to the M2 candidate set, round 6 and the sibling-state sweep
status: active
topic: m2-closing-review-corrections
scope: operational
owner: claude
decided_at: 2026-09-05
relations:
  - type: amends
    target: DEC-20260314-C
  - type: amends
    target: DEC-20260315-A
  - type: amends
    target: DEC-20260315-B
  - type: amends
    target: DEC-20260404-A
  - type: amends
    target: DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6
  - type: amends
    target: DEC-20260430-A
evidence:
  - archive/sessions/2026-09-05-m2-closing-review-round-6.md
  - docs/decisions/records/DEC-20260314-C.md
  - docs/decisions/records/DEC-20260314-F.md
  - docs/decisions/records/DEC-20260315-A.md
  - docs/decisions/records/DEC-20260315-B.md
  - docs/decisions/records/DEC-20260330-B.md
  - docs/decisions/records/DEC-20260404-A.md
  - docs/decisions/records/DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6.md
  - docs/decisions/records/DEC-20260420-I--notion-34867c87082c81c8b9d4c6b5568bbcef.md
  - docs/decisions/records/DEC-20260422-H.md
  - docs/decisions/records/DEC-20260430-A.md
  - docs/decisions/id-collisions.yaml
  - archive/sessions/strale-spike-correlation-analysis-2026-04-08.md
  - archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-K.md
  - https://app.notion.com/32367c87082c81eda40dfa601fd6b444
  - https://app.notion.com/32367c87082c81bfaf90c949e06b8594
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

The quotation convention for this candidate set is unchanged from
`DEC-20260905-F`: normalize quotation and source before comparing:
transliterate `€` to `EUR`, `×` to `x`, `≥` to `>=`, `≤` to `<=`, `→` to
`->`, `…` to `...`; lowercase; delete every character that is not a
letter or digit. A quotation is faithful when its normalized form is a
substring of the normalized source (an ellipsis splits it into ordered
segments). Punctuation, dashes, quote marks, case, markdown emphasis,
whitespace and the punctuation at a truncation point are therefore never
defects. Defects are: a word inserted, dropped, replaced or reordered; a
composite presented as one quotation; a quotation whose named source does
not contain it (wrong file, function, document, record or row field); a
quotation attributed to a source outside the repository, the parsed row,
the sibling frontend checkout or a merged pull request; and any false
statement about repository state at the commit. The relation rule is also
unchanged: a relation declared in a record's frontmatter whose basis is
stated in an amending record, or narrated in the target record's own
body, is substantiated; the narration need not live in the source
record's own body once that basis is stated somewhere reachable from the
record.

The statements listed below are withdrawn from the named records and
replaced by the facts stated here. None of them touches a relation edge;
this record substantiates nothing new (see Not adopted).

### `DEC-20260314-C`

1. Withdraws, as the record has it (Consequences section, lines 53-56):
   "**No recurring multi-LLM evaluation job exists in the repository
   today.** A search of `docs/`, `apps/api/src/`, `apps/api/scripts/`,
   and `scripts/` for \"multi-llm,\" \"multi_llm,\" \"multiLLM,\" or
   \"ChatGPT evaluation\" found no match." Fact: a case-insensitive search
   of `docs/` alone for `multi-llm` or `ChatGPT evaluation`
   (`grep -ril "multi-llm\|ChatGPT evaluation" docs/`) returns two files
   at this commit: `docs/decisions/records/DEC-20260314-C.md` (this
   record's own file, which contains the literal search terms in its own
   prose) and `docs/project/DECISIONS.md` (the generated index, which
   mirrors this record's text). The "found no match" claim is false as
   literally stated. The underlying substantive conclusion, no
   recurring, scheduled multi-model evaluation mechanism exists in the
   codebase, as opposed to this record's own descriptive prose about that
   absence, is not disturbed; only the search-result claim itself is
   corrected. Evidence: `docs/decisions/records/DEC-20260314-C.md`;
   `docs/project/DECISIONS.md`.

### `DEC-20260315-A`

2. Withdraws, as the record has it (line 61-62): "This matches the row's
   own description of the target (\"free capabilities via MCP without
   auth\")," attributing the quoted phrase to `DEC-20260315-A`'s own
   Notion row (page `32367c87082c81eda40dfa601fd6b444`). Fact: that row's
   actual Rationale field reads in full: "Auth wall is the #1 blocker for
   autonomous agent activity. 2027.dev AX research confirms auth causes
   40% of agent failures. Without zero-auth free tier, content launch
   drives discovery but agents can't convert to usage. Moving Sprint 9F
   from Phase B Launch +3d to Launch day/+1d." No substring of that field
   contains "free capabilities via MCP without auth" under any
   normalization. The phrase belongs to a different record's row instead:
   `DEC-20260314-F`'s own row (page `32367c87082c81bfaf90c949e06b8594`)
   states "Applied to: Sprint 9F (elevated to 5 free capabilities via MCP
   without auth), Sprint 11E (new AX audit item), Sprint 11D (tool
   description optimization)." This is a misattribution: the quoted
   phrase is real, just not on the row this record names, borrowed
   instead from a sibling record's row. The underlying substantive claim
   (a zero-auth capability set exists) is not disturbed; only the
   exact-quotation attribution is wrong. Evidence:
   `docs/decisions/records/DEC-20260315-A.md`; `DEC-20260314-F`'s row
   Rationale field (page `32367c87082c81bfaf90c949e06b8594`).

### `DEC-20260315-B`

3. Withdraws, as the record has it (Consequences section, lines 74-75):
   "this row's own text does not name it (it predates that decision by 16
   days)," referring to the gap between `DEC-20260315-B`'s own
   `decided_at` and `DEC-20260330-B`'s `decided_at`. Fact:
   `docs/decisions/records/DEC-20260315-B.md`'s frontmatter reads
   `decided_at: 2026-03-15`; `docs/decisions/records/DEC-20260330-B.md`'s
   frontmatter reads `decided_at: 2026-03-30`. The interval between
   2026-03-15 and 2026-03-30 is 15 days, not 16, by direct date
   subtraction. A false arithmetic claim about a date interval the record
   itself computes and states as fact. The underlying substantive point
   (this record does not create a relation edge to `DEC-20260330-B`
   because its own text never names it) is not disturbed; only the
   day-count is wrong. Evidence:
   `docs/decisions/records/DEC-20260315-B.md` frontmatter `decided_at:
   2026-03-15`; `docs/decisions/records/DEC-20260330-B.md` frontmatter
   `decided_at: 2026-03-30`.

### `DEC-20260404-A`

4. Withdraws, as the record has it (Consequences section, lines 72-73):
   "A repository-wide search for `TDQS` finds only
   `archive/sessions/audit/2026-04-04-strale-mcp-tdqs-rewrite.md`." Fact:
   a repository-wide, case-sensitive search for `TDQS` at this commit
   returns at least seven files, including
   `archive/sessions/strale-spike-correlation-analysis-2026-04-08.md:207`
   ("Apr 4 | Reddit/X distribution, Glama TDQS, usage milestone | session
   | Yes — records spike as it happens"), a genuinely independent,
   unrelated mention rather than a self-reference or the generated
   index. The "finds only" claim is false as literally stated. The
   underlying substantive conclusion (no repository document records
   Glama's own re-scan result) is not disturbed; only the search-result
   claim itself is corrected. Evidence:
   `archive/sessions/strale-spike-correlation-analysis-2026-04-08.md:207`.

### `DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6`

5. Withdraws, as the record has it (Context section, lines 104-106): the
   nearest export evidence for the "direct connections only" rule's
   substance is "a different row entirely, `DEC-20260420-I` (page id
   `34867c87082c81c8b9d4c6b5568bbcef`, itself an unresolved collision id
   in a later G2 batch)." Fact: at this commit,
   `docs/decisions/id-collisions.yaml` records the `DEC-20260420-I`
   collision with `resolution_status: resolved`, and both of its rows
   carry `disposition: formal_record` with existing formal-record files
   on disk (`docs/decisions/records/DEC-20260420-I--notion-34867c87082c81c8b9d4c6b5568bbcef.md`
   and `docs/decisions/records/DEC-20260420-I--notion-34867c87082c8172a41ac4c9d52904de.md`).
   The claim that this page id is "itself an unresolved collision id in a
   later G2 batch" is false: the collision resolved in a later batch, and
   by the time of this review both formal records already exist on
   `main`. The identical claim also appears, unwithdrawn, in the
   corresponding resolution report,
   `archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-H.md`
   line 82, that report is an archive file, never amended, so this item
   corrects only the protected record's statement; the resolution
   report's own stale prose is noted, not corrected, in the Consequences
   section below. The underlying substantive point this record makes (the
   two exported rows under this collision do not themselves state the
   "direct connections only" rule; `DEC-20260420-I`'s row does) is not
   disturbed; only the "unresolved"/"later G2 batch" characterization of
   `DEC-20260420-I` is wrong. Evidence: `docs/decisions/id-collisions.yaml`
   (the `DEC-20260420-I` block);
   `docs/decisions/records/DEC-20260420-I--notion-34867c87082c81c8b9d4c6b5568bbcef.md`;
   `docs/decisions/records/DEC-20260420-I--notion-34867c87082c8172a41ac4c9d52904de.md`.

### `DEC-20260430-A`

6. Withdraws, as the record has it (Consequences section, lines 81-83):
   "The source partially superseded vendor-selection content in
   `DEC-20260420-K`, whose display ID is an unresolved collision, and the
   Movitz-dependent path in the unique but unmigrated `DEC-20260422-H`."
   Fact: at this commit, `docs/decisions/id-collisions.yaml`'s
   `DEC-20260420-K` entry carries `resolution_status: resolved` with two
   `disposition: formal_record` rows, and
   `docs/decisions/records/DEC-20260422-H.md` exists as a migrated
   bare-keyed formal record (`record_key: DEC-20260422-H`, `decided_at:
   2026-04-22`, `migration_status: candidate`). Both halves of the
   sentence are false: `DEC-20260420-K` is resolved, not unresolved, and
   `DEC-20260422-H` is migrated, not unmigrated. This was not withdrawn by
   any of `DEC-20260905-B` through `-F` (`DEC-20260905-F` items 1-2
   substantiate this same record's two *different* undeclared relations,
   to `DEC-20260428-A`/`DEC-20260428-B`; neither touches this sentence).
   The corresponding archive resolution report,
   `archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-K.md`,
   already flags this exact gap in its own "Forward correction: two
   protected records call this collision unresolved" section ("Both
   statements are now stale... Neither protected record is edited; this
   is a forward, prose-only correction"), confirming the gap was
   identified when `DEC-20260420-K` was resolved and left for a future
   closing-review round to formally withdraw, this item is that
   withdrawal. `DEC-20260422-H.md` itself quotes this same sentence from
   `DEC-20260430-A` as its own cited evidence rather than stating the
   claim independently, so quoting it there remains a faithful quotation
   of what `DEC-20260430-A` says, not a fresh defect in `DEC-20260422-H`.
   Evidence: `docs/decisions/id-collisions.yaml` (the `DEC-20260420-K`
   block); `docs/decisions/records/DEC-20260422-H.md`;
   `archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-K.md`.

## Context

Round 6 of the M2 closing independent review ran at commit
`ff8a1384694532d037c5fc0b27588ee93daf63ae`, after `DEC-20260905-F`
withdrew round 5's confirmed statements and substantiated the two relation
gaps that round raised. Six fresh, read-only reviewers applied the
quotation convention `DEC-20260905-D`/`-E`/`-F` state, in six partitions
covering a contiguous slice of the corpus each. P1, P3, P4 and P6 passed
with no confirmed defects; P2 failed with four findings (the search claim
withdrawn in item 1, the misattribution withdrawn in item 2, the
arithmetic slip withdrawn in item 3, and the search claim withdrawn in
item 4); P5 failed with one finding (the collision-state claim withdrawn
in item 5). All nine gates ran clean at this commit (exit 0 each; `npm run
context:check`, `npm run context:test`, `node --test
scripts/decision-records.test.mjs scripts/m2-closure-register.test.mjs`,
`node scripts/m2-closure-verify-private-rows.mjs`, `npm run
programs:check`, `npm run codex:check`, `npm run receipts:check`, `node
apps/api/scripts/check-pii.mjs --strict`, `node
apps/api/scripts/check-no-committed-secrets.mjs`); the run is valid.

This record's own reconciliation additionally ran the operator checker
over the full corpus (236 records, 1128 spans, 1029 faithful, **99**
residual) at this same commit and read every residual directly against
its named source rather than trusting the checker's best-match guess.
Full reconciliation, entry by entry:
`scratchpad/residual-reconciliation-round6.md` (not committed; this
summary is the citable form). Of the 99: **0** were already withdrawn by
`DEC-20260905-B` through `-F`; **0** are new defects (none of this
round's five confirmed findings surfaced as a checker residual, none is
a >=25-character double-quoted span the checker extracts; all five
surface only through direct reading); **99** were checker misses, each
faithful to a located and quoted source (11 individually verified across
11 files, all of them already independently verified by this round's own
P2, P3 and P4 partition reports; 88 self-referential parsing artifacts
inside `DEC-20260905-C.md` (82), `DEC-20260905-D.md` (1), and
`DEC-20260905-F.md` (5, newly appearing this round because `-F` merged to
`main` after round 5 and the checker now parses it for the first time),
the same class rounds 3 through 5's reconciliations described and
quantified for the immutable already-merged withdrawal records).

Beyond the six partitions' own coverage, this record additionally ran two
corpus-wide sweeps the round-6 brief specifically required, because this
round's confirmed findings and the prior four rounds' pattern share one
class: **a statement about another record's or a collision's state that
was true when the record was drafted and is false at this commit.**
First, a sibling-state sweep: every file under `docs/decisions/records/`
and every `archive/sessions/*-decision-collision-resolution-*.md` file
was grepped, case-insensitively, for fourteen patterns describing another
record's or collision's existence or state (`no record`, `no formal
record`, `not yet`, `unresolved`, `documented_only`, `documented-only`,
`pending`, `does not exist`, `has no record`, `is not a formal record`,
`not part of this batch`, `later batch`, `later G2`, `awaits`, `will be`,
`record for`), and every hit naming a different record's id was read in
context and checked against `docs/decisions/id-collisions.yaml` and the
`docs/decisions/records/` directory at this commit. 350 raw hits, 37 in
the records corpus and 8 in the archive corpus surviving a mechanical
filter for genuine cross-record state claims; full sweep:
`scratchpad/sibling-state-sweep.md` (not committed). It found exactly one
statement not already withdrawn by a prior round and not still true: item
6 above, `DEC-20260430-A`'s stale claim about `DEC-20260420-K` and
`DEC-20260422-H`, independently corroborated by the
`DEC-20260420-K` resolution report's own "Forward correction" section.
Second, a search-claim sweep: every record was grepped, case-insensitively,
for thirteen patterns describing a repository search's claimed result
(`no match`, `zero match`, `no hits`, `returns nothing`, `finds only`,
`only match`, `only hit`, `only file`, `nothing else`, `no other`, `sole
`, `the only place`, `grep`), and every statement naming a search and its
result was re-run at this commit, same pattern and scope as the record
describes; full sweep: `scratchpad/search-claim-sweep.md` (not
committed). It found no defect beyond the two search-claim findings (items
1 and 4) partition P2 had already found independently.

All reviewed records are `status: active`.
`validateProtectedDecisionChange` in `scripts/decision-records-lib.mjs`
refuses any commit that changes an active record's protected sections
(`DECISION_ACTIVE_BODY_CHANGED`) or its metadata
(`DECISION_ACTIVE_METADATA_CHANGED`). Correcting a false or misattributed
statement inside an active record therefore cannot go through an edit to
that record; it goes through a new record (this one) that names the
withdrawal, the same mechanism `DEC-20260905-B` through `-F` used for
rounds 1 through 5.

## Rationale

One amending record per remediation round keeps history immutable, keeps
the reviewer trail intact, keeps the validator's protections untouched,
and matches a stable evidence file
(`archive/sessions/2026-09-05-m2-closing-review-round-6.md`). The
quotation convention, restated unchanged from `DEC-20260905-F`, continues
to remove reviewer disagreement about what counts as a defect; it does
not weaken the bar on words, attribution, or facts. A checker residual is
a defect unless the source text that contains it is located and quoted;
this record's own reconciliation quotes or names the located source for
every one of the 99 checker misses it reports.

Statements about sibling records and collisions are the one class of
defect that ages as the candidate set grows: a statement true when its
record was drafted (a collision genuinely unresolved, a sibling record
genuinely unmigrated) becomes false purely because a *later* batch
resolved the collision or migrated the sibling record, with no change to
the citing record's own text. Four closing-review rounds have now each
found a fresh instance of this exact class (rounds 2, 3 and 4, per
`DEC-20260905-C`, `-D` and `-E`'s Consequences sections, and this round;
round 5's `DEC-20260430-A` relation-basis gap and `DEC-20260505-H`
misattribution were a different class). Rather than wait
for a seventh, eighth, or ninth round to find the next one by sample, this
record sweeps the entire corpus once, exhaustively, against the
collision registry and the records directory as they stand today, so that
round 7 inherits a corpus checked for this specific failure mode in full
rather than only in the six partitions' own contiguous slices.

## Consequences

(a) Round 7 treats a statement withdrawn here, in `DEC-20260905-B`,
`DEC-20260905-C`, `DEC-20260905-D`, `DEC-20260905-E`, or `DEC-20260905-F`
as corrected, and applies the quotation convention stated above.

(b) Amended records keep their text; the correction lives only here.

(c) **Not adopted**: findings, observations, and one explicitly requested
item this record deliberately did not treat as a withdrawal or
substantiation, with reasons:

- **The two relation-substantiation items the round-6 brief requested for
  `DEC-20260430-A`'s `related_to` relations to `DEC-20260428-A` and
  `DEC-20260428-B`.** These two relations were already fully substantiated
  by `DEC-20260905-F` items 1-2, from `DEC-20260430-A`'s own Context
  section read alongside each target's unique frontmatter `title` and
  `topic`. This round's own P3 partition independently re-verified that
  substantiation is accurate (its "Rule (a) cross-check" table entry for
  `DEC-20260430-A`: "verified the record's own Context sentence... names
  both targets by unique subject matter... Substantiation accurate") and
  did not flag either relation as a fresh gap; no partition in round 6
  raised these relations as unsubstantiated. I additionally dumped
  `DEC-20260430-A`'s own Notion row (page `35267c87082c81eca01cf6eedb5eafeb`)
  directly: its Decision field reads only "v1 + v1.1 vendor stack
  canonicalized to Vendor Roster" and its Rationale field is null, the
  row itself supplies no basis for either relation beyond what
  `DEC-20260905-F` already used from the record's own body. Writing a
  second substantiation item here would not record a new basis; it would
  restate an existing one under a fresh item number, which is not what
  "substantiates, not withdraws" means in `DEC-20260905-E`'s or `-F`'s own
  usage of that phrase. Not included.
- **Convention-covered classes among the 99 checker residuals** (a
  record's own grep/search pattern presented as a quotation of its
  methodology rather than a source's content; a record's own rhetorical
  paraphrase or illustrative phrasing not attributed to any source; a
  self-quotation of the same record's own earlier text; a quotation
  verified faithful to a file not listed in the record's own `evidence:`
  array but named in its prose; the 88 self-referential parsing artifacts
  inside `DEC-20260905-C.md`, `-D.md`, and `-F.md`). These are checker
  misses this record's reconciliation locates a source for
  (`scratchpad/residual-reconciliation-round6.md`); none is a withdrawal
  target.
- **`DEC-20260321-A`'s repo-wide `schedule_tier`/`scheduleTier` grep**
  (`scratchpad/search-claim-sweep.md`): the record's list of 7 files
  omits several other files matching the same pattern
  (`generate-*-tests.ts`, `test-scheduler.ts`, `test-runner.ts`,
  `internal-tests.ts`), but the record explicitly qualifies its search as
  "excluding tests" and separately covers `internal-tests.ts` in a
  narrower two-file search in the same paragraph; a plausible,
  non-disqualifying reading (excluding anything test-named by
  convention), the same kind of borderline scope question P2 and P4 both
  judged non-disqualifying elsewhere this round. Not withdrawn.
- **`DEC-20260406-E`'s "Market Context"/"Competitive Landscape" search
  claim**: one incidental case-insensitive hit exists
  (`archive/sessions/audit-output/_partial_mt_enumeration.md`, an
  unrelated Kyckr-pricing sentence), the same nuance round 4's own closing
  review already found and judged non-disqualifying since it does not
  disturb the record's actual claim. Following that precedent, not
  re-flagged.
- **Stale resolution-report statements, superseded by the registry at
  this commit.** `archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-H.md:82`
  carries the identical "unresolved collision id... later G2 batch"
  characterization of `DEC-20260420-I` that item 5 above withdraws from
  the protected record; and
  `archive/sessions/2026-09-05-decision-collision-resolution-DEC-20260420-K.md`'s
  own "Forward correction" section narrates (accurately, as a
  description) the exact gap item 6 above withdraws. Resolution reports
  are archive files and are never amended; these two are noted here, not
  corrected, since correcting them would mean editing an archive file the
  Report Filing Convention and the immutability rule both treat as a
  closed historical record.
- **Unverifiable production/database-state claims** carried over
  unresolved from `DEC-20260905-B` through `-F`'s own "Not adopted" lists.
  This record does not resolve them and none was asserted as a fact to
  withdraw.
- **Point-in-time counts correctly dated in a record's own text**, carried
  over from the prior amending records' own "Not adopted" lists on the
  same ground: STALE_COUNT by nature, not FALSE_CLAIM.

(d) The operator checker `scripts/m2-quote-fidelity.mjs`'s residual list
at commit `ff8a1384694532d037c5fc0b27588ee93daf63ae`
(`scratchpad/fidelity-residuals-round6.json`, 99 residual spans)
reconciles as: **0** already withdrawn by `DEC-20260905-B` through `-F`;
**0** new defects; **99** checker misses, each faithful to a located
source (11 individually verified across 11 files; 88 self-referential
artifacts inside `DEC-20260905-C.md` (82), `-D.md` (1), and `-F.md` (5)).
Full reconciliation, entry by entry:
`scratchpad/residual-reconciliation-round6.md` (not committed; this
summary is the citable form).

**Relation to `DEC-20260314-C`**: withdraws the false "found no match"
search-result claim in item 1 above.

**Relation to `DEC-20260315-A`**: withdraws the misattributed "free
capabilities via MCP without auth" quotation in item 2 above.

**Relation to `DEC-20260315-B`**: withdraws the false 16-day arithmetic
claim in item 3 above.

**Relation to `DEC-20260404-A`**: withdraws the false "finds only"
search-result claim in item 4 above.

**Relation to `DEC-20260420-H--notion-34867c87082c81c6a58dfbc5f46ed3f6`**:
withdraws the false "unresolved collision id in a later G2 batch"
characterization of `DEC-20260420-I` in item 5 above.

**Relation to `DEC-20260430-A`**: withdraws the false "unresolved
collision"/"unmigrated" characterization of `DEC-20260420-K` and
`DEC-20260422-H` in item 6 above; substantiates nothing new for this
record (see Not adopted).

## Reversal conditions

A later closing-review round that shows a correction in this record to be
wrong produces a further amending record, never an edit to this one. If a
reviewed in-place correction mechanism for candidate records is adopted
before the M4 cutover, that mechanism may supersede this record. The M4
cutover itself does not reverse this record.
