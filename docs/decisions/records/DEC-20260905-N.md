---
record_key: DEC-20260905-N
id: DEC-20260905-N
title: Closing-review corrections to the M2 candidate set, round-12 findings
status: active
topic: m2-closing-review-corrections
scope: operational
owner: claude
decided_at: 2026-09-06
relations:
  - type: amends
    target: DEC-20260905-J
  - type: amends
    target: DEC-20260505-E--notion-35767c87082c813481a8efa27ea37438
  - type: amends
    target: DEC-20260505-D--notion-35767c87082c81d3897fe47a2ec7a4c1
evidence:
  - docs/decisions/records/DEC-20260905-J.md
  - docs/decisions/records/DEC-20260812-A.md
  - docs/decisions/records/DEC-20260420-E--notion-34867c87082c81d5a898f48cc1554086.md
  - docs/decisions/records/DEC-20260505-E--notion-35767c87082c813481a8efa27ea37438.md
  - docs/decisions/records/DEC-20260505-D--notion-35767c87082c81d3897fe47a2ec7a4c1.md
  - config/env-manifest.yaml
  - archive/sessions/2026-09-05-m2-closing-review-round-12.md
migration_status: candidate
authority_scope: none
authority_active: false
phase: M2
---

> [!CAUTION]
> **M2 CANDIDATE RECORD — NOT ACTIVE PROJECT AUTHORITY.**
> The recorded decision status is historical source data. Existing `AGENTS.md`, `CLAUDE.md`, and Notion-backed workflows remain authoritative until M4 cutover.

## Decision

The quotation convention for this candidate set is unchanged from
`DEC-20260905-M`: normalize quotation and source before comparing:
transliterate `€` to `EUR`, `×` to `x`, `≥` to `>=`, `≤` to `<=`, `→` to
`->`, `…` to `...`; lowercase; delete every character that is not a
letter or digit. A quotation is faithful when its normalized form is a
substring of the normalized source (an ellipsis splits it into ordered
segments). Punctuation, dashes, quote marks, case, markdown emphasis,
whitespace and the punctuation at a truncation point are therefore never
defects. Defects are: a word inserted, dropped, replaced or reordered; a
composite presented as one quotation; a quotation whose named source does
not contain it (wrong file, function, document, record or row field); a
quotation attributed to a source outside the repository, the parsed row,
the sibling frontend checkout or a merged pull request; and any false
statement about repository state at the commit. `DEC-20260905-M` adds one
clause, unchanged here: a double-quoted span a record attributes to no
source, and that is not presented as the words of a row, file, page or
person, is the record's own wording, judged as prose, not as a quotation.

The statements listed below are withdrawn from the named records and
replaced by the facts stated here.

### `DEC-20260905-J`

1. Withdraws, as the record has it (item 20, lines 424-425): "not
   language `docs/decisions/records/DEC-20260812-A.md` or the
   direction-plan document itself uses." Fact:
   `docs/decisions/records/DEC-20260812-A.md` line 83 (Reversal
   conditions) is part of the sentence "Supersede or amend this decision
   if measured operating evidence invalidates the program sequence or
   thresholds, or a founder-approved company direction replaces the
   library-as-product strategy." The literal hyphenated phrase
   "library-as-product" is present in that file, confirmed by `git grep
   -n -i "library-as-product" HEAD -- docs/decisions/records/DEC-20260812-A.md`
   (one hit: `docs/decisions/records/DEC-20260812-A.md:83:replaces the
   library-as-product strategy. Do not weaken the carried-forward`).
   `DEC-20260420-E--notion-34867c87082c81d5a898f48cc1554086`'s attribution
   of the phrase to `DEC-20260812-A` (existing record) is therefore
   faithful under the quotation convention (a normalized substring of the
   named source), and item 20's claim that the phrase is "not language
   `docs/decisions/records/DEC-20260812-A.md` ... uses" is false and is
   withdrawn. Item 20's separate observation that
   `docs/strategy/2026-08-05-direction-plan.md` itself never uses the
   compound phrase is true and stands; nothing in
   `DEC-20260420-E--notion-34867c87082c81d5a898f48cc1554086` is withdrawn
   by this record.

### `DEC-20260505-E--notion-35767c87082c813481a8efa27ea37438`

2. Withdraws, as the record has it (line 43, Consequences):
   "`config/env-manifest.yaml` carries eight `HMRC_*` rows". Fact:
   `config/env-manifest.yaml` at HEAD carries exactly seven `HMRC_*`
   rows, confirmed by `git grep -c "name: HMRC_" HEAD --
   config/env-manifest.yaml` (prints `7`) and by name:
   `HMRC_CLIENT_ID`, `HMRC_CLIENT_SECRET`, `HMRC_REQUESTER_VRN`,
   `HMRC_SANDBOX_CLIENT_ID`, `HMRC_SANDBOX_CLIENT_SECRET`,
   `HMRC_TEST_VRN`, `HMRC_USE_SANDBOX`, the same seven names the
   record's own sentence already enumerates. The count is seven, matching
   the record's own enumeration; "eight" is withdrawn.

### `DEC-20260505-D--notion-35767c87082c81d3897fe47a2ec7a4c1`

3. The record's frontmatter (line 9) states `decided_at: 2026-05-05`.
   Fact: the record's own source row (Notion page
   `35767c87082c81d3897fe47a2ec7a4c1`, read only via `dump_rows.py
   PAGE:35767c87082c81d3897fe47a2ec7a4c1`) has its `Date` property at
   `2026-05-04` and a `createdTime` of `2026-05-05 09:35:36Z`; the
   record's own Consequences section (line 46) already says the row's
   Outcome was "recorded 2026-05-04". The sibling record for the same
   collision pattern,
   `DEC-20260505-E--notion-35767c87082c81e2ba50d630d0b95f9d`, sets
   `decided_at: 2026-05-04` from an identical row Date property. Active
   records are immutable (`DECISION_ACTIVE_BODY_CHANGED`), so
   `DEC-20260505-D--notion-35767c87082c81d3897fe47a2ec7a4c1.md` is not
   edited. Withdrawn without editing the record: reading its
   `decided_at` as the row's `Date` property. The row's decision date is
   2026-05-04; the record's `decided_at` of 2026-05-05 follows the id's
   own date and the page's creation time, not the row's `Date` property;
   readers take 2026-05-04 as the decision date. This changes no id, key
   or ordering.

## Context

Round 12 of the closing review ran at commit
`fdf915652fe04a6e4d56ea6a7ab6a54e074b8d4d` (`DEC-20260905-M`'s merge
commit), with six read-only partition reviewers (P1 through P6) and a
gate run. Four partitions (P1, P2, P3, P4) passed clean and all nine
gates were clean; partition P5 found item 1 above by checking the
amending records' own facts against their sources; partition P6 found
items 2 and 3 above. Active records are immutable
(`DECISION_ACTIVE_BODY_CHANGED`), so none of `DEC-20260905-J`,
`DEC-20260505-E--notion-35767c87082c813481a8efa27ea37438`, or
`DEC-20260505-D--notion-35767c87082c81d3897fe47a2ec7a4c1` is edited; the
corrections are this record instead, the same mechanism every prior
round in this chain used. The six partition reports and the gate output
from this round are archived by this PR at
`archive/sessions/2026-09-05-m2-closing-review-round-12.md`.

## Rationale

A correction that asserts a source lacks a phrase the source contains is
wrong and is withdrawn like any other false statement; the original
attribution in item 1 was faithful and the corpus must say so. A count
that contradicts the record's own enumeration, or a date that
contradicts the record's own row, is corrected the same way.

## Consequences

(a) Round 13 treats a statement withdrawn by `DEC-20260905-B` through
`-M` or this record as corrected.

(b) The amended records (`DEC-20260905-J`,
`DEC-20260505-E--notion-35767c87082c813481a8efa27ea37438`,
`DEC-20260505-D--notion-35767c87082c81d3897fe47a2ec7a4c1`) keep their
text; the corrections live only here.

(c) **Not adopted**: none. This record's three findings are all
withdrawals or corrections verified against a source in the repository
or the parsed Notion export; nothing this record's own review found is
left uncorrected and unwithdrawn.

**Relation to `DEC-20260905-J`**: withdraws item 20's claim that
"library-as-product" is not language `DEC-20260812-A.md` uses, and
states that `DEC-20260420-E--notion-34867c87082c81d5a898f48cc1554086`'s
attribution is faithful to `DEC-20260812-A.md` line 83; item 20's true
observation about the direction-plan document itself is undisturbed and
so is every other item in `DEC-20260905-J`.

**Relation to `DEC-20260505-E--notion-35767c87082c813481a8efa27ea37438`**:
withdraws the "eight `HMRC_*` rows" count in item 2 above and states the
count is seven, matching the record's own enumerated list; the record's
underlying Consequences finding (that the row's `Outcome` field is null
and no HMRC verdict is recorded) is undisturbed.

**Relation to `DEC-20260505-D--notion-35767c87082c81d3897fe47a2ec7a4c1`**:
withdraws the reading of the record's `decided_at` frontmatter field as
the row's `Date` property, per item 3 above; the record's decision
content, evidence, and every other field are undisturbed.

## Reversal conditions

A later closing-review round that shows a correction in this record to
be wrong produces a further amending record, never an edit to this one.
If a reviewed in-place correction mechanism for candidate records is
adopted before the M4 cutover, that mechanism may supersede this record.
The M4 cutover itself does not reverse this record.
