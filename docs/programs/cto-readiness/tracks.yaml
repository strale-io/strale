# CTO-readiness program — track register.
# Validated by `npm run programs:check` (tests: `npm run programs:test`).
# Human view: PROGRAM.md; this file wins on any disagreement.
# Status enum: queued | active | blocked | founder_gated | done | rehomed
# Exactly one track is `active` at any time. A `blocked` or `founder_gated`
# track must name its blocker. A `done` track must list evidence paths that are
# tracked regular files on main. A `rehomed` track must name where it went.
# Only `done` satisfies a dependency. `program_status` is active (exactly one
# active track), paused (no active track; at least one founder_gated or blocked
# track and nothing runnable), or complete (every track done or rehomed).
# Every track declares `gate`: none (not sequenced by the migration), m2 (M2
# work), m2-exit (the single M2 exit gate), post-m2 (M3 and later). The M2
# closure register's validator refuses any post-m2 track that is active or
# done while a blocking M2 gap remains, or that does not depend (transitively)
# on the m2-exit track; adding a track cannot bypass this because the field
# is required.
schema_version: 1
program: cto-readiness
program_status: active
updated: 2026-09-02
tracks:
  - id: T1
    title: M2 closure audit
    status: done
    gate: m2
    depends_on: []
    owner: session
    next_action: >
      Done. The remaining M2 work is the exit-gap list in
      docs/project/m2-closure-register.yaml (G1, G2, G3, G9 blocking); the
      next collision-free Decision batch is named there. The row-level
      projection for rows not yet public lives in the private archive and is
      verified with node scripts/m2-closure-verify-private-rows.mjs.
    resume_file: handoff/_general/from-code/2026-09-02-t1-m2-closure-audit.md
    exit:
      - schema-validated disposition register committed with exact counts per disposition
      - every inventory entry and Decision source row has a disposition, provenance, and rationale
      - explicit M2 exit-gap list and next collision-free Decision batch named
      - tests fail on missing entries, unsupported dispositions, invalid sources, duplicate identities, count drift, or silent removal
      - M1 bare inventory contract unchanged
    evidence:
      - docs/project/m2-closure-register.yaml
      - handoff/_general/from-code/2026-09-02-t1-m2-closure-audit.md

  - id: T2
    title: Repo hygiene sweep
    status: done
    gate: none
    depends_on: []
    owner: session
    next_action: >
      Done. Remote branches other than main: 0; archive tags: 45; linked
      worktrees: 2 (the morning check-in's and the active batch). The trunk
      is detached at origin/main because the check-in worktree holds main;
      T3 re-points the daily run and returns the trunk to main. Seven
      unmapped plain directories and three frontend orphan worktrees are
      listed in the report for a reviewed decision in T3, with four plain
      copies under C:/tmp found by the review. See the report.
    resume_file: archive/sessions/2026-09-02-t2-repo-hygiene-sweep-report.md
    exit:
      - every remote and local branch either deleted with landed-evidence or listed with a reason
      - every worktree either removed or mapped to an active session in WORKTREES.md
      - shared checkout clean and idle at origin/main (back on main when T3 re-points the daily run), its former uncommitted work preserved under archive tags
      - rescue snapshots triaged; none silently discarded
    evidence:
      - archive/sessions/2026-09-02-t2-repo-hygiene-sweep-report.md
      - handoff/_general/from-code/2026-09-02-t2-repo-hygiene-sweep.md

  - id: T3
    title: Automatic session-end gate
    status: done
    gate: none
    depends_on: [T2]
    owner: session
    next_action: >
      Done. Shipped in PR #480 (15fe6f78): npm run handoff:check behind
      Claude Code hooks, the Codex notify wrapper and .codex/hooks.json,
      git hooks via core.hooksPath, and the weekly stale-branches
      workflow (proven with a planted branch: issue #481 opened, then
      closed after deletion). The Stop hook blocked a live session twice
      the same day on exactly that planted branch. Still unverified: the
      Codex layer live (notify wrapper re-linked in ~/.codex/config.toml
      after merge; first Codex turn here after the quota returns on
      2026-09-07 must show .claude/state/handoff/last-codex.json).
    resume_file: handoff/_general/from-code/2026-09-02-t3-session-end-gate.md
    exit:
      - planting each failure mode makes handoff:check fail with a one-line fix; a clean repo passes
      - a Claude Code Stop with a dirty worktree is blocked; SessionStart prints the active track
      - a direct push to main from a workstation is refused by the git hook
      - the Codex notify wrapper writes the status file and the orientation script prints it
      - CI job lists branches older than seven days with no open PR and opens one tracking issue
      - WORKTREES.md describes the allowed set and the trunk is on main
    evidence: 
      - handoff/_general/from-code/2026-09-02-t3-session-end-gate.md
      - scripts/handoff/handoff-check.mjs
      - .github/workflows/stale-branches.yml

  - id: T11
    title: Website repo hygiene and design preservation
    status: done
    gate: none
    depends_on: [T3]
    owner: session
    next_action: >
      Done 2026-09-02. Frontend preserved and swept (strale-frontend PR #22,
      release preserve-2026-09-02, archive tags), folders removed, the same
      session-end gate installed there, and the monorepo direction filed as
      DEC-20260902-A in the Notion Decisions DB (Petter confirmed 2026-09-02)
      and listed in CLAUDE.md; the repo-native record follows through the M2
      closure path with that page as its source. Left for the next website
      session: strale-frontend PR #5, the untracked og-strale-final.png in its
      trunk, the review-evidence zip in the redesign-v2 worktree.
    resume_file: handoff/_general/from-code/2026-09-02-t11-website-hygiene.md
    exit:
      - every website folder outside a git checkout is preserved (tag or release asset with checksum) and removed
      - strale-frontend has only main, work branches with a named owner or open PR, and archive tags
      - strale-frontend runs the same session-end gate as this repository
      - the monorepo direction is recorded in ROADMAP section 7 and its decision record is filed through the M2 path
    evidence: 
      - handoff/_general/from-code/2026-09-02-t11-website-hygiene.md
      - docs/project/ROADMAP.md

  - id: T12
    title: Research contract
    status: done
    gate: none
    depends_on: [T3]
    owner: session
    next_action: >
      Done 2026-09-02 (PR #486): front-matter schema, research:check and
      research:test in CI, generated index, all 37 files migrated, IDEAS.md,
      identical paragraph in CLAUDE.md and AGENTS.md. Five research-looking
      files under docs/strategy and docs/audits are reported as warnings for
      a later migration batch.
    resume_file: archive/sessions/2026-09-02-t12-research-contract-plan.md
    exit:
      - research:check passes on main and each planted failure mode fails it with a one-line fix
      - every docs/research Markdown file carries valid front matter and appears in the generated index
      - an active decision citing a superseded research file fails the check
      - identical research-and-ideas paragraph in CLAUDE.md and AGENTS.md, inventory regenerated
    evidence: 
      - handoff/_general/from-code/2026-09-02-t12-research-contract.md
      - scripts/check-research.mjs
      - docs/research/README.md

  - id: T13
    title: Design tokens as data, candidates, promotion
    status: done
    gate: none
    depends_on: [T12]
    owner: session
    next_action: >
      Done 2026-09-02 (PR #487): design/tokens/active.json per surface with
      provenance and adopted_by, two candidates with status, PROVENANCE.md,
      generated internal-report tokens, design:check and design:test in CI
      (per-surface promotion gate, ratcheted allowlist, apps/web joins the
      lint scope when it exists), DESIGN-SYSTEM.md on one page, identical
      paragraph in CLAUDE.md and AGENTS.md.
    resume_file: archive/sessions/2026-09-02-t13-design-tokens-plan.md
    exit:
      - design:check passes on main and each planted failure mode fails it with a one-line fix
      - the internal report consumers render from active.json through the generated file
      - both candidates and the provenance record carry a status; DESIGN-SYSTEM.md is one page
      - identical design paragraph in CLAUDE.md and AGENTS.md, inventory regenerated
    evidence: 
      - handoff/_general/from-code/2026-09-02-t13-design-tokens.md
      - design/tokens/active.json
      - scripts/check-design.mjs

  - id: T14
    title: Cheap extras — environment manifest, model registry, claims register
    status: done
    gate: none
    depends_on: [T13]
    owner: session
    next_action: >
      Done 2026-09-02 (PR #488): config/env-manifest.yaml (127 rows, 27
      holders unknown and listed for Petter in the handoff), generated
      .env.example files, apps/api/src/lib/models.ts as the only home of a
      model id (93 call sites), docs/company/claims.yaml (27 rows) with a
      scanner that was hollow on first review and is now proven on planted
      claims; the drift it then exposed (retired SQS score in README.md and
      two package READMEs) was narrowed to what the code returns. The
      founder's 2026-09-02 plan is complete; T4 resumes.
    resume_file: archive/sessions/2026-09-02-t14-cheap-extras-plan.md
    exit:
      - env:check, models:check and claims:check pass on main and each planted failure mode fails with a one-line fix
      - zero model-id literals outside models.ts; every process.env name has a manifest row
      - claims register seeded from the handoff audit's forbidden claims and the drift patterns
      - identical paragraph in CLAUDE.md and AGENTS.md, inventory regenerated
    evidence: 
      - handoff/_general/from-code/2026-09-02-t14-cheap-extras.md
      - config/env-manifest.yaml
      - apps/api/src/lib/models.ts
      - docs/company/claims.yaml

  - id: T4
    title: Remediation program closure
    status: blocked
    blocker: production read-only access needs the root .env and apps/api/.env that were lost on 2026-09-02 (DQ-29); Petter recreates them, then T4 resumes
    gate: none
    depends_on: []
    owner: session
    next_action: >
      Record WP10's overdue seven-day cadence verdict from production evidence;
      close WP9 observation; establish the Railway proxy-hop count read-only
      for VERIFY-IP; decide WP15's database-lane item; ship or formally defer
      WP17; run WP13's reachability triage; re-home WP14 and WP16.
    resume_file: docs/remediation/CURRENT-STATE.md
    exit:
      - WP9 and WP10 carry a terminal status in their package files
      - VERIFY-IP has a recorded answer with the query that produced it
      - WP12, WP13, WP15, WP17 each have a dated status that is not PLANNED or NOT_STARTED
      - WP14 appears in tracks.yaml as founder_gated with the exact evidence requested
      - WP16 appears as T9 with its own plan pointer
      - PACKAGE-GRAPH.yaml reconciled against package files
    evidence: []

  - id: T5
    title: CTO-readable repository structure
    status: active
    gate: none
    depends_on: [T2]
    owner: session
    next_action: >
      (Dependency on T4 dropped 2026-09-02: T5 moves nothing under docs/,
      so remediation closure need not precede it; T4 is blocked on DQ-29.)
      Per the stored plan: README.md rewritten top-down for a stranger;
      docs/README.md with an authority status per subtree and docs:check;
      generated archive/ and handoff/ indexes with a --check in CI; root
      reduced to canon, LICENSE, build files and registry manifests (the two
      checklists move under docs/governance/protocols with references
      updated; manifests-drafts archived if nothing reads it);
      docs/project/STRUCTURE.md records every deviation from the migration
      plan's target layout with a reason and owner track; LESSONS F5 gains
      the standing rule that no checker ships before it has failed on a
      planted case.
    resume_file: archive/sessions/2026-09-02-t5-cto-readable-structure-plan.md
    exit:
      - README.md routes a stranger from product to code to process in one read
      - docs/README.md indexes every docs/ subtree with one line each
      - archive/ and handoff/ have generated indexes and no unindexed files
      - repo root contains only files listed in the CLAUDE.md Report Filing Convention as top-level canon
      - each deviation from migration plan section 5 is listed with a reason
    evidence: []

  - id: T10
    title: M2 exit-gap closure
    status: queued
    gate: m2-exit
    depends_on: [T1]
    owner: session
    next_action: >
      Close the blocking gaps listed in docs/project/m2-closure-register.yaml
      (G1 pending rows, G2 Notion collisions, G3 the cross-surface collision,
      G9 the closing review) through contradiction-checked Decision batches,
      the collision identity mechanism, and one closing independent review of
      the complete M2 candidate set. Start with the next batch the register
      names. This track is the M2 exit gate: T6 cannot start before it is
      done, and the closure register's validator refuses a state where this
      track is done, or any post-m2 track is active or done, while a blocking
      gap remains.
    resume_file: handoff/_general/from-code/2026-09-02-t1-m2-closure-audit.md
    exit:
      - counts.exit_gaps.blocking in the closure register is 0
      - the closing independent review of the full M2 candidate set is archived under archive/sessions
    evidence: []

  - id: T6
    title: M3 repo-native workflows
    status: queued
    gate: post-m2
    depends_on: [T1, T5, T10]
    owner: session
    next_action: >
      Per migration plan section 10 M3. First sub-batch is the
      vendor-current-state shadow replacement named by the vendor-stack
      closeout.
    resume_file: docs/strategy/2026-08-31-repo-native-operating-model-migration.md
    exit:
      - repo-native replacements testable without changing existing authority
      - no path writes to both repo-native truth and Notion
      - scheduled and deployment mechanisms verified, not assumed
      - every mandatory protocol represented in the coverage manifest
      - old entrypoints remain in force
    evidence: []

  - id: T7
    title: M4 atomic authority cutover
    status: queued
    gate: post-m2
    depends_on: [T3, T6]
    owner: session
    next_action: >
      Per migration plan section 10 M4. Prepare the single cutover PR, then
      set this track to founder_gated with the exact reviewed commit named as
      the blocker; on the founder's yes, merge and verify both entrypoints
      load the same truth.
    resume_file: docs/strategy/2026-08-31-repo-native-operating-model-migration.md
    exit:
      - both tools load the same project truth and pass entrypoint parity
      - no active state read from or written to Notion
      - all guards blocking
    evidence: []

  - id: T8
    title: M5 to M7 closeout
    status: queued
    gate: post-m2
    depends_on: [T7]
    owner: session
    next_action: Per migration plan section 10 M5, M6, M7.
    resume_file: docs/strategy/2026-08-31-repo-native-operating-model-migration.md
    exit:
      - legacy authorities archived with zero broken Charter links
      - clean-session acceptance passed on Claude Code and Codex from the same commit
      - migration plan archived with review and acceptance evidence
    evidence: []

  - id: T9
    title: Discovery and retrieval authority (WP16)
    status: queued
    gate: post-m2
    depends_on: [T7]
    owner: session
    next_action: Open its own program plan under docs/programs/ when T7 is done.
    resume_file: docs/remediation/PACKAGE-GRAPH.yaml
    exit:
      - own program plan and register exist
    evidence: []
