# Work that shipped without the Codex review path, and owes it.
#
# Founder decision 2026-09-03: the Codex quota is out until 2026-09-07 and
# waiting costs more than proceeding. Work continues, reviewed by a fresh
# read-only Claude agent that did not author the batch, and every batch that
# would otherwise have gone to Codex is recorded here until it does.
#
# This is a debt register, not a to-do list. A row is added by the session
# that merges the work, not by the session that plans it. A row closes one of
# two ways: a Codex verdict, archived under archive/ and cited by path, or a
# waiver by the founder naming the decision. Never by time passing, never by
# a second Claude review, and — enforced against git, not asserted — never by
# editing this file back. `npm run codex:check` compares the register with
# its merge-base on main and refuses a deleted row, a status that moves
# backward or reopens a closed row, a changed commit, an edited verdict, and a
# review date pushed later without a founder decision; on the file itself it
# refuses a commit git does not have, a reviewed row whose evidence file
# is not a verdict for it (it must say VERDICT: <verdict> and name the
# commit), a waiver by anyone but the founder, a decision id this repository
# does not record (a formal record or a bold entry in CLAUDE.md), and a row
# still pending after `review_by`. Three things it cannot see, and the
# pull-request review is the guard for each: a batch never added; a drain
# that reaches main through review (the base moves forward when a merge
# lands, as it does for receipts); and a decision entry added to CLAUDE.md
# in the same change that cites it — the checker proves a decision is
# recorded in a reviewed file, not that the founder made it.
#
# Drain order is `priority`, then oldest first. Highest first: these are the
# ones where a Claude-only review is least adequate.

schema_version: 1
policy:
  decision: DEC-20260903-A
  raised: "2026-09-03"
  review_by: "2026-09-07"
  routing: >
    Until Codex returns, a fresh read-only Claude agent that did not author
    the batch performs the independent review, and the batch is recorded
    here. When Codex returns, drain this register before starting new work
    that would add to it. A Codex verdict of FAIL on merged work opens a
    remediation batch; it does not revert automatically.

entries:
  - id: CX-36
    subject: "PR #598 — content redaction moved weekly -> daily, a per-run-ceiling backlog warning, the job-coordinator clamp that makes any interval change take effect, a cross-invocation SEC rate gate on company-fundamentals, and the pii-redact retention disclosure"
    commit: ff9482b5
    merged: "2026-09-06"
    priority: high
    status: pending
    why_codex: >
      It changes how long personal data actually lives — the same reason CX-3
      is high — and it does so by editing the shared job coordinator, which
      every recurring job on the platform goes through. Two Claude rounds were
      each necessary and neither sufficient: round 1 found the cadence fix
      would have done nothing for a week (the clamp keys on a column
      consumeDueSlot never writes) and that three of the batch's own numbers
      were wrong; round 2 found the corrected numbers had not reached the file
      whose whole purpose is to be the arithmetic of record. Two rounds of the
      same model each finding real defects in the other's wake is the argument
      for a third opinion, not against it.
    what_to_attack: >
      The ON CONFLICT clamp in consumeDueSlot. It was simulated against all
      seven consumeDueSlot rows before merge and changed exactly one, but the
      author wrote the simulation: re-derive it. Specifically whether an
      unchanged interval is genuinely a no-op for every task (it depends on
      consumeDueSlot writing next_run_at and last_started_at in the same
      statement), whether a lengthened interval can push a run out, and whether
      any path other than job-coordinator.ts writes job_schedule. Then the five
      sibling purge loops in data-retention.ts, which this cadence change also
      accelerates sevenfold and which carry the identical invisible-ceiling
      shape with no backlog warning — their backlogs measured zero on
      2026-09-06, so the question is whether that holds under load, not whether
      it held that day. Finally weekly-sweep, the other consumeDueSlot 7-day
      task with last_finished_at NULL, which inherited the same PR #376
      latency and has not been checked.
    claude_verdicts: 2 rounds — FAIL (five blockers, all fixed), then PASS WITH FINDINGS (one documentation must-fix, fixed in the follow-up carrying this row)
  - id: CX-35
    subject: "PR #592 — german-company-data's ambiguity refusal was recognised by none of the three health consumers, plus the GOALS.md correction that found it"
    commit: 91f8ecaf
    merged: "2026-09-06"
    priority: high
    status: pending
    why_codex: >
      Two reasons, and the second is the stronger one. First, this is the
      same defect class as CX-34 one day later — a refusal message that the
      circuit breaker, the quality floor and the trust surfaces all fail to
      recognise — which is the open three-strike family (LESSONS F1), and
      german-company-data is the last registry that kept its own pickByName
      rather than the shared one, so the 2026-08-14 consolidation never
      reached it. Second, and this is what a different model is actually
      for: the batch overturns a conclusion this repository had already
      written down and acted on. GOALS.md recorded the 2026-08-24 German
      workload as 159 refusals and named repackaging refusals the highest-
      value open product change; grouping the same rows by error string
      says 127 of the 159 were the vendor's own HTTP 402 and only 32 were
      refusals. One session produced both the original reading and its
      reversal, from the same database, a day apart. A second model should
      say which reading is right rather than taking the newer one on trust.
    what_to_attack: >
      The census first, because everything else rests on it. Re-derive the
      2026-08-24 german-company-data breakdown independently — 36 completed,
      127 HTTP 402, 17 no-confident-match, 15 tied-refusal out of 195 — and
      say whether grouping by error string can mislead here (are the 402s
      really the vendor's, or could a 402 be raised on our own side before
      the call went out?). Then the €39.00-at-list demand figure, which is
      list price times requested calls and therefore assumes every refused
      query would have resolved; say whether that overstates the case for
      buying capacity. Then apps/api/src/capabilities/german-company-data.ts:
      confirm the new "Ambiguous German company name" wording is claimed by
      no earlier branch of classifyTransactionFailure (INTERNAL_RE,
      TIMEOUT_RE, UPSTREAM_RE all run first), and that no caller depended on
      the old count-first wording. Finally the two files this batch
      deliberately did NOT change — finnish-company-data.ts:113 and
      spanish-company-data.ts:176 throw plain Errors whose messages happen to
      open with "Ambiguous "; is "recognised by string is enough" actually
      true on every path, including the async one where only the message
      survives?
    claude_verdicts: 1 round, PASS, no blockers, two nits — one taken (an unanchored test regex that matched the pre-fix wording too), one acknowledged
  - id: CX-34
    subject: "PR #582 — eight keyless agent-data capabilities (clinical-trials-search, doi-resolve, citation-graph, cert-transparency-search, host-exposure-lookup, breach-exposure-check, fda-safety-search, company-fundamentals) plus a corrected false claim on password-strength"
    commit: 4529f778
    merged: "2026-09-06"
    priority: high
    status: pending
    why_codex: >
      Eight new executors on the paid rail at once, and the Claude review
      round found a defect class the repo has an open three-strike
      investigation on: twenty-two refusal messages that the circuit
      breaker, the quality floor and the trust surfaces all read as
      internal errors, which is the 2026-08-14 french-company-data incident
      (LESSONS F1/F9) reproduced eight times over. It was caught by review,
      not by authoring, which is the argument for a second opinion rather
      than against one. The batch also changed a customer-facing claim on a
      capability it did not otherwise touch (password-strength said
      "Password is NOT stored" while transactions.input retains it for 90
      days) — a narrowing correction under DEC-20260822-A part 2, but a
      claim change nonetheless.
    what_to_attack: >
      Whether every refusal site in the eight executors is recognised by
      all three consumers on the ASYNC and x402 paths, not just the sync
      one — the house-style rewrite was verified through
      classifyTransactionFailure/isUserInputError/categorizeError in
      new-capabilities-refusal.test.ts, but only sync execution was
      exercised live. Whether company-fundamentals can exceed the SEC's 10
      requests/second under concurrent customer calls (9-13 requests per
      call, concurrency 5, no cross-request limiter; an SEC block is not a
      refusal and would open the breaker) — raised by the Claude review and
      deliberately left open. Whether the manifests' `guaranteed` fields
      are actually always present on a successful response, which no gate
      proves. Whether cert-transparency-search's two upstreams can
      disagree about the same domain in a way the provenance line does not
      make visible.
    claude_verdicts: 1 round, FAIL with five blockers, all addressed; re-review not run before merge
  - id: CX-33
    subject: "PR #569 — M2 G2 batch 5: the last nine ID collisions (DEC-20260505-D/E, DEC-20260507-A/B/C, DEC-20260508-B/C, DEC-20260512-A, DEC-20260513-F) resolved atomically; G2 closed"
    commit: f7b1f651
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      The last collision batch, after which the closure register records
      G2 resolved and only G9 blocks the M2 exit. Includes the three-row
      DEC-20260508-C and the superseded NL row. Whether each disposition is
      justified by the parsed rows, each code claim traced to the file, and
      the G2 closure justified by the register's own counts are the Codex
      judgements.
    what_to_attack: >
      Each record against its Notion row (by page id, parsed); each
      registry entry against the report and the register rows; the zero
      unresolved_collision count, the G2 closure and exit_gaps counts; the
      register arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-32
    subject: "PR #564 — startup-migration block 0112 lists the eight free-public-API capabilities and clears the fabricated dependency_health rule on six of them"
    commit: 135e556d
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      It overrides an automatic quality gate to put capabilities on sale, and
      it does so from a block that runs at boot, where a throw crash-loops
      production. The promotion job's 95%-over-40-results bar was unreachable
      for these eight (a fabricated `status` assertion pinned six at 80%, and
      the free_quota 24h suite floor put 40 results ten days out), so the
      block moves lifecycle_state, visible and x402_enabled directly. Whether
      the evidence offered — live execution of all eight against their
      upstreams, green known_answer suites, free keyless sources — is an
      adequate substitute for the gate it replaces is exactly the judgement a
      second provider should make, and a Claude session both made the call and
      reviewed it.
    what_to_attack: >
      Whether runMigration0112_promoteFreeApiEight in
      apps/api/src/lib/startup-migrations.ts can throw for any reachable
      production state (the capabilities_no_half_quarantine CHECK, the
      UPDATE...RETURNING result shape against postgres-js, jsonb exact-match
      semantics); whether the known_overlaps justification for
      capabilities.lifecycle_state holds, given migration-ledger-lib.mjs
      allowlists by column name and never verifies the blocks list; whether
      the block's statements auto-committing individually (getDb() is a
      pooled non-transactional client) can leave capabilities listed with
      their promotion events missing, and what the floor and promotion job
      then do; and whether removing the `status` check from six suites while
      keeping it on cve-details is the correct partition — verify against
      each executor's actual output, not the manifests.
    claude_verdicts: 1 round, PASS with 3 findings (untested deactivation_reason interlock, incomplete known_overlaps blocks list, ledger rows_affected mismatch); all three fixed in 7eec1f46 before merge
  - id: CX-31
    subject: "PR #563 — M2 G2 batch 4: seven ID collisions (DEC-20260420-I/J/K, DEC-20260421-A/B/C/D) resolved atomically with thirteen source-qualified records and one documented-only row"
    commit: 6f2dec52
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      Irreversible collision resolutions covering the direct-connections
      doctrine row, the Payee Assurance conditions retired by
      DEC-20260812-A, and the superseded DEC-20260420-J twin. Whether each
      disposition and relation edge is justified by the parsed rows and
      each code claim traced to the file is the Codex judgement.
    what_to_attack: >
      Each of the thirteen records against its Notion row (by page id,
      parsed); the DEC-20260420-J documented_only rationale; the prose
      corrections for DEC-20260422-H and DEC-20260430-A; the register
      arithmetic and digests via scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-30
    subject: "PR #560 — M2 G2 batch 3: seven ID collisions (DEC-20260406-C, DEC-20260409-C, DEC-20260420-D/E/F/G/H) resolved atomically with twelve source-qualified records and two documented-only rows"
    commit: 8deb296b
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      Irreversible collision resolutions including the first two
      documented_only rows (a superseded twin and an identical-title
      duplicate) and the DEC-20260420-H finding that the export holds no
      row stating the social-platform scraping rule that three merged
      records attribute to that id. Whether the duplicate comparison, the
      dispositions and the range-expanded relation edges are justified by
      the parsed rows is the Codex judgement.
    what_to_attack: >
      Each of the twelve records against its Notion row (by page id,
      parsed); the DEC-20260420-D comparison against both rows; the
      DEC-20260420-H report against DEC-20260420-I's text; the relation
      edges derived from the Option C row's "through DEC-20260420-G" range;
      the register arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-29
    subject: "PR #507 — the caller-class partition (harness / account / x402 / anonymous), the who-called operator script, and the 2026-09-04 and 2026-09-05 operating runs"
    commit: b9739d12
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      This batch changes what the word "customer" means to every consumer of
      lib/metrics, and externalCustomers() is load-bearing for revenue,
      concentration and the quality floor's population. Two defects of the
      same shape were already found in it by review rather than by tests,
      after it had been written specifically to prevent that shape: our own
      health_probe rows classified as anonymous customers (77 of the 78 rows
      in the reproduced window), and the anonymous class labelled "x402"
      while carrying 59 free-tier rows with no wallet behind them. A third of
      the same kind is what a Claude-only review is least able to rule out.
    what_to_attack: >
      Whether the four classes are still exhaustive and mutually exclusive
      over every row of transactions, including any status value the schema
      comment does not list; whether callerClass (TS) and callerClassSql
      agree on all four branches AND on branch order, since health_probe and
      x402 are both decided before user_id is looked at; whether adding the
      status predicate to externalCustomers moved any of its seven consumers
      in a direction other than removing our own rows — identityCoverage
      counts rows and is the one that moves; and whether the mutation
      evidence cited in the handoff (four planted mutations, each caught by
      exactly one test) covers SQL semantics or only rendered SQL text, since
      the suite renders strings and never executes the CASE.
    claude_verdicts: 1 round, FAIL then all findings fixed (see PR body)
  - id: CX-28
    subject: "PR #555 — M2 G2 batch 2: six ID collisions (DEC-20260320-C/J/K, DEC-20260405-B, DEC-20260406-A/B) resolved atomically with twelve source-qualified records"
    commit: 036f207d
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      Irreversible collision resolutions (the registry's merge-base rules
      refuse a regression). The batch also asserts that the KYB and Invoice
      Verify solution families are absent from the catalogue source and
      partly retired by name, and that the au-company-data hotfix's
      .d.ts filter no longer exists. Whether each disposition is justified
      by its own source text and each code claim traced to the file is the
      Codex judgement.
    what_to_attack: >
      Each of the twelve records against its Notion row (by page id,
      parsed); each registry entry against the report and the register
      rows; the solution-catalogue and retirement-script claims; the
      auto-register.ts, pep-check.yaml, platform-facts.ts and
      solution-executor.ts claims; the register arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-27
    subject: "PR #553 — M2 G2 batch 1: five ID collisions (DEC-20260225-P-c5d6, DEC-20260303-A, DEC-20260304-A, DEC-20260304-B, DEC-20260304-C) resolved atomically with ten source-qualified records"
    commit: 81d59db4
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      The first collision resolutions after the DEC-20260502-A worked
      example. Each resolution is irreversible history (the registry's
      merge-base rules refuse a regression), so whether each row's
      disposition is justified by its own source text, whether the
      source-qualified keys map one-to-one, and whether the resolution
      reports' implementation reconciliation is traced to code are the
      Codex judgements.
    what_to_attack: >
      Each of the ten records against its Notion row (by page id, parsed);
      each registry entry against the report and the register rows; every
      bare collided id in existing records and prose; the register
      arithmetic (unresolved_collision minus ten, formally_migrated plus
      ten) and the recomputed scope_date_digest via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-26
    subject: "PR #551 — M2 batch 18: thirteen May 2026 decisions (DEC-20260505-A/B/C, DEC-20260507-I/J, DEC-20260510-A, DEC-20260511-B/E/F, DEC-20260513-A/B/C/D) migrated to formal candidate records; G1 closed"
    commit: 6832cccb
    merged: "2026-09-05"
    priority: high
    status: pending
    why_codex: >
      The last G1 batch, after which the closure register records G1
      resolved. The records judge the scheduled_testing_eligible decoupling
      against the boot-time derivation that CLAUDE.md documents, the
      lifecycle and tiebreaker removals, the breaker pins, and the digest
      rot. Whether each claim is traced to code, whether each edge is
      source-stated, and whether G1's closure is justified by the register's
      own counts are the Codex judgements.
    what_to_attack: >
      Each record against its Notion source; every Consequences claim
      against the file it names (startup-migrations.ts block 0066,
      matching.ts, test-runner.ts and circuit-breaker.ts, test-scheduler.ts,
      the three country manifests, VOICE.md); every relation edge; the
      register arithmetic, the zero not_yet_reconciled count, the G1
      closure and exit_gaps counts, and the recomputed scope_date_digest via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-25
    subject: "PR #549 — M2 batch 17: twelve April to early-May 2026 decisions (DEC-20260406-E, DEC-20260409-A/B/D, DEC-20260410-A, DEC-20260413-A, DEC-20260415-A/B, DEC-20260422-H, DEC-20260427-H/I, DEC-20260503-B) migrated to formal candidate records"
    commit: 892a1f75
    merged: "2026-09-05"
    priority: medium
    status: pending
    why_codex: >
      Twelve records including the SQS redesign row itself and the two
      catalogue deactivation rows, whose Consequences enumerate the
      DEACTIVATED map's entries and each named slug's state; plus the one
      temporary row in G1. Whether each slug claim is traced to the map and
      the manifests, and whether each edge is source-stated, is the Codex
      judgement.
    what_to_attack: >
      Each record against its Notion source; every Consequences claim
      against the file it names (auto-register.ts DEACTIVATED map, auth.ts
      signup, entity-validation.ts, test-scheduler.ts, schema.ts residual
      SQS columns, VOICE.md, DECISION-QUEUE.md); every relation edge; the
      register arithmetic and the recomputed scope_date_digest via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-24
    subject: "PR #547 — M2 batch 16: twelve March 2026 decisions (DEC-20260314-F/G, DEC-20260315-A/B/H/I, DEC-20260317-A/F/G/H, DEC-20260321-A, DEC-20260329-A) migrated to formal candidate records"
    commit: ccece2b2
    merged: "2026-09-05"
    priority: medium
    status: pending
    why_codex: >
      Twelve records whose Consequences sections judge what survived the
      SQS deletion and the never-executed provider programme (billing on
      upstream failure, the digest cadence, the publication threshold, the
      evidence weighting, the schedule_tier ordering, the palette's move
      into design tokens). Whether each claim is traced to code rather
      than remembered, and whether each edge is source-stated, is the Codex
      judgement.
    what_to_attack: >
      Each record against its Notion source; every Consequences claim
      against the file it names (do.ts settle path, x402-gateway.ts,
      daily-digest.ts, trust-labels.ts, trust-grade.ts, onboard.ts,
      test-scheduler.ts, design/tokens/active.json, the frontend Index
      page at its cited sha); every relation edge; the register arithmetic
      and the recomputed scope_date_digest via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-23
    subject: "PR #544 — M2 batch 15: fourteen March 2026 launch-month decisions (DEC-20260302-C/D, DEC-20260303-C, DEC-20260305-E/F, DEC-20260306-G/H, DEC-20260309-H, DEC-20260310-E/F, DEC-20260313-E, DEC-20260314-A/B/C) migrated to formal candidate records"
    commit: fdb38591
    merged: "2026-09-05"
    priority: medium
    status: pending
    why_codex: >
      Fourteen launch-month records whose Consequences sections judge what
      the SQS deletion and later work did with each (no ranking-explanation
      page, no blog route, no disclaimer field on today's risk
      capabilities, 36 web-provider importers against the row's 47). Six
      records cite the frontend by commit-qualified cross-repo path.
      Whether each claim is traced to code and each cross-repo sha resolves
      is the Codex judgement.
    what_to_attack: >
      Each record against its Notion source; every Consequences claim
      against the file it names (web-provider.ts and its importers,
      dependency-manifest.ts, daily-digest.ts, seller-rank.ts, suggest.ts,
      onboard.ts, the frontend Header, Methodology, Index, CapabilityDetail
      and Terms pages at the cited shas); the single relation edge; the
      register arithmetic and the recomputed scope_date_digest via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-22
    subject: "PR #542 — M2 batch 14: fourteen MVP launch and scaling decisions of 2026-02-25/27 (DEC-20260225-P-m5n6/o7p8/y1z2, DEC-20260226-P-q1r2/s3t4/u5v6/w7x8, DEC-20260227-P-a1b2/i9j0/m3n4/o5p6/q7r8/s9t0/u1v2) migrated to formal candidate records"
    commit: cc936d78
    merged: "2026-09-05"
    priority: medium
    status: pending
    why_codex: >
      Fourteen founding-week records whose Consequences sections judge what
      survived (no LLM fuzzy input despite CLAUDE.md, TED shipped, the EU AI
      Act columns and their writer, provider growth never executed, no
      reputation engine, no Visa TAP). Six relation edges, two of them
      beyond the brief's list. Whether each claim is traced to code rather
      than remembered, and whether each edge is source-stated, is the Codex
      judgement.
    what_to_attack: >
      Each record against its Notion source; every Consequences claim
      against the file it names (swedish-company-data.ts, ted-procurement
      manifest, schema.ts and do.ts, the SDK package.json, the MCP and A2A
      surfaces); the six relation edges; the register arithmetic and the
      recomputed scope_date_digest via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-21
    subject: "PR #539 — M2 batch 13: fourteen founding decisions of 2026-02-24/25 (DEC-20260224-P-a1b2/c3d4/e5f6/g7h8, DEC-20260225-P-a3b4/e7f8/g9h0/i1j2/k3l4/m1n2/q3r4/s5t6/u7v8/w9x0) migrated to formal candidate records"
    commit: 8ebfafa9
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      Fourteen founding-era records whose Consequences sections judge what
      survived of each decision (the pivot, the seed set, the payment
      architecture, keypair identity, provider-lite). Whether each "status
      on 2026-09-04" claim is traced to code or a later record rather than
      remembered is the Codex judgement.
    what_to_attack: >
      Each record against its Notion source; every Consequences claim
      against the file it names (manifests, x402 gateway, auth, langchain
      packages, solutions route, VOICE.md, claims.yaml); the relation edges;
      the register arithmetic and the recomputed scope_date_digest via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-20
    subject: "PR #537 — M2 batch 12: twelve vendor and registry sourcing decisions (DEC-20260320-E, DEC-20260405-A, DEC-20260505-G/H, DEC-20260506-G, DEC-20260507-D/E/F/G/H, DEC-20260508-A/D) migrated to formal candidate records"
    commit: e4b6376a
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      Twelve records whose Consequences sections state that production
      sourcing diverged from the decided paths (Bolagsverket shipped, the
      Openapi aggregator for BG/CY/LU/HU, no Implisense or Kyckr code). Ten
      relation edges. The largest batch so far; fidelity across twelve
      sources and the divergence claims are Codex judgements.
    what_to_attack: >
      Each record against its Notion source; the ten edges and their types;
      the executor and manifest claims for swedish-, german-, bulgarian-,
      cypriot-, luxembourgish- and hungarian-company-data; the env-manifest
      rows quoted; the register arithmetic and the recomputed
      scope_date_digest via scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-19
    subject: "PR #532 — M2 batch 11: seven Counterparty Assurance v1 decisions (DEC-20260515-A/B/C, DEC-20260518-A/B/C/D) migrated to formal candidate records"
    commit: 7ca52b3c
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      Seven records for a product framing the readiness program retired;
      each states the retirement and what survives in code. The batch also
      carries four relation edges, one of them the orchestrator's reading of
      a "partial supersession" as amends. Fidelity and relation typing are
      Codex judgements.
    what_to_attack: >
      Each record against its Notion source; the amends edge from
      DEC-20260515-A to DEC-20260430-A (partial supersession, target stays
      active); the code claims about Cobalt, us-court-search, ubo_availability
      and the missing Digiteal handler; the register arithmetic and digests
      via scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-18
    subject: "PR #529 — M2 batch 10: six distribution and x402-rail decisions (DEC-20260313-F, DEC-20260324-A, DEC-20260324-C, DEC-20260330-B, DEC-20260404-A, DEC-20260416-A) migrated to formal candidate records"
    commit: d858a695
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      Records about the distribution surfaces and the x402 rail, with live
      anchors (the CDP facilitator, the AgentCash discovery compatibility,
      the root registry manifests) and two drift findings. Whether the
      records' code claims are traced to callers rather than comments is
      the judgement this batch series has been losing rounds on.
    what_to_attack: >
      Each "status on 2026-09-04" claim against the files it names
      (server.json vs packages/mcp-server/package.json, x402-gateway.ts,
      x402-gateway-v2.ts, context7.json, docs/ide-rules); the absence of
      relation edges; the register arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-17
    subject: "PR #526 — M2 batch 9: six trust and quality display decisions (DEC-20260305-G, DEC-20260306-D, DEC-20260313-C, DEC-20260316-A, DEC-20260316-B, DEC-20260323-A) migrated to formal candidate records"
    commit: deb08103
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      Six records whose subject, the SQS and its display, was deleted on
      2026-05-05; each Consequences section says what survives. Whether the
      records keep the historical Decision text intact while stating the
      deletion honestly, and whether the dead computeTrustGrade finding is
      right, are judgements for a second model.
    what_to_attack: >
      Each record against its Notion source; the "status on 2026-09-04" code
      claims (public-trust.ts header, trust-grade.ts dead code, the residual
      columns, capability_health not renamed); the absence of relation edges;
      the register arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-16
    subject: "PR #524 — M2 batch 8: five onboarding-pipeline decisions (DEC-20260309-G, DEC-20260318-A, DEC-20260318-B, DEC-20260320-A, DEC-20260411-B) migrated to formal candidate records"
    commit: 746c3beb
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      These records describe the pipeline the Capability Onboarding Protocol
      (DEC-20260320-B) enforces, and one of them (the 12-category risk
      framework) is a rule the repository never implemented. Whether the
      records say that plainly without weakening the historical text is a
      fidelity judgement for Codex.
    what_to_attack: >
      Each record against its Notion source for any dropped step, count or
      flag; the "status on 2026-09-04" claims against apps/api/scripts/onboard.ts,
      the readiness checker and gate5-path-coverage.ts; the absence of any
      relation edge (the sources cite no decision ids); the register
      arithmetic and digests via scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round (see PR body)
  - id: CX-15
    subject: "PR #521 — M2 batch 7: five pricing decisions (DEC-20260302-A-0001, DEC-20260308-1, DEC-20260320-F, DEC-20260411-A, DEC-20260513-E) migrated to formal candidate records"
    commit: c309a8ca
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      Pricing records with live contradictions: the vendor cost basis moved
      (OpenSanctions to Dilisense), the compliance-screening prices never
      reached the EUR 0.25 the 2026-03 decision set, and one relation
      (DEC-20260411-A amends DEC-20260302-A-0001) rests on subject restatement
      rather than an explicit citation. Whether those are stated honestly and
      whether the amends edge is source-stated enough are Codex judgements.
    what_to_attack: >
      Each record against its Notion source for any dropped price, margin or
      competitor figure; the "status on 2026-09-04" price claims against the
      manifests; the amends edge; DEC-20260513-E's summarized Outcome (the
      Claude review found it was first presented as a quote); the register
      arithmetic and digests via scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round, PASS (one should-fix applied by the orchestrator before merge)
  - id: CX-14
    subject: "PR #518 — eight keyless free-API capabilities (OpenAlex, arXiv, PubMed, HN, SEC EDGAR, NVD, USGS) from the largest x402 buyer's basket"
    commit: 5bf6c43d
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      New executors on the money path that build upstream URLs from caller
      input. Three Claude rounds were needed: round 1 found query-parameter
      smuggling through the DOI, round 2 found dot-segment path collapse
      through the same function after the first fix. A model that did not
      write either fix should look for the third variant, and at the seven
      other executors the rounds spent less time on.
    what_to_attack: >
      apps/api/src/capabilities/paper-details.ts resolveWorkIdentifier /
      encodeDoiPath (percent-encoded dots, backslashes, unicode, doi.org
      URLs); every URLSearchParams construction in the other seven executors
      for a parameter that changes the upstream route; readJsonWithLimit /
      readTextWithLimit on every body; the manifests' guaranteed fields
      against a real empty-result response (zero hits) for each search
      capability; the sec-ticker-index probe cost at four runs a day.
    claude_verdicts: 3 rounds — FAIL, FAIL, PASS; every finding fixed

  - id: CX-13
    subject: "PR #517 — M2 batch 6: provenance and jurisdiction rows (DEC-20260422-D, DEC-20260425-A, DEC-20260425-B) migrated to formal candidate records"
    commit: 167e46e0
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      DEC-20260425-A's record states that the decision's core mechanism
      (manifest-declared jurisdictions) was never implemented and that the
      audit field is still derived by a heuristic. A record that says its
      own decision did not ship is exactly where a second model should check
      the claim and the wording against the source and the code.
    what_to_attack: >
      DEC-20260425-A's five-option Rationale for any dropped reason; the
      "status on 2026-09-04" block against apps/api/src/lib/provenance-builder.ts
      getProcessingJurisdictions(); the affirms edge to DEC-20260425-B; the
      22-executor provenance-field count; the register arithmetic and digests
      via scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round, FAIL on a stale index and one omitted relation, both applied by the orchestrator before merge
  - id: CX-12
    subject: "PR #515 — M2 batch 5: capability lifecycle patterns (DEC-20260421-J, DEC-20260421-L, DEC-20260422-B) migrated to formal candidate records"
    commit: fa869015
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      Three operating patterns transcribed with a contradiction the worker
      found itself: the exemplar retired capability was reactivated eight
      days later. Whether the records state that honestly without rewriting
      the historical Decision text is a fidelity judgement for Codex.
    what_to_attack: >
      The numbered steps, the Type 1 to 5 tree and the four tombstone reasons
      against the sources; the bd25bc57 reactivation claim; the amends and
      related_to edges; the register arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round, PASS (one nit)
  - id: CX-11
    subject: "PR #510 — drizzle-orm 0.38.4 -> 0.45.2 (T17 batch 2), with the DrizzleQueryError unwrap module and five routed readers"
    commit: 47d69050
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      A semver-major on the ORM every query goes through. The first pass
      claimed no source change was needed; CI and two Claude reviews found
      five readers of err.code that had silently stopped seeing the Postgres
      error (duplicate signup as a 500, DB errors logged as unknown, the
      wallet timeout 503, the settlement race discharge). Three rounds is a
      signal that a different model should read the whole surface once more.
    what_to_attack: >
      apps/api/src/lib/db-error.ts (the unwrap and the narrowed PostgresError
      heuristic); any remaining reader of a caught DB error's code, message,
      detail or constraint in apps/api/src and scripts that the sweep table
      in handoff/_general/from-code/2026-09-04-t17-drizzle-upgrade.md missed
      (grep the cast forms); the expectDbRejection test helper for hollowness;
      the append-only migration blocks (none may have changed); drizzle-kit
      0.31.10 in the CI integration-db lane.
    claude_verdicts: 3 rounds (FAIL, FAIL, PASS by the orchestrator after the Sonnet pool rate-limited)
  - id: CX-10
    subject: "PR #513 — M2 batch 4: three engineering-convention rows (DEC-20260419-A, DEC-20260420-A, DEC-20260511-C) migrated to formal candidate records"
    commit: 8b124678
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      Decision-record fidelity is the work the 2026-09-01 routing reserved
      for Codex. These three records transcribe engineering conventions that
      the code still enforces, and one of them (DEC-20260511-C) carries a
      dated contradiction with today's tree (drizzle-kit back for the CI
      test lane only). Whether that contradiction is stated honestly and
      completely is a judgement worth a second model.
    what_to_attack: >
      Each record's five sections against its Notion source for any dropped
      rule, path, commit id or option; the DEC-20260511-C "status on
      2026-09-04" block against apps/api/drizzle.config.ts, package.json and
      .github/workflows/ci.yml; the single affirms edge; the register
      arithmetic and digests via scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round, PASS (three nits)
  - id: CX-9
    subject: "PR #511 — DEC-20260422-A cross-surface collision resolved (G3 stage 2): protocol record DEC-20260422-A--git-3b256587, Notion row evidence-only"
    commit: cf20869f
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      A formal record was transcribed from CLAUDE.md's Distribution PR
      Integrity Protocol, the protocol Codex itself helped shape after the
      pydantic-ai incident, and one collision was resolved by choosing an
      evidence-only disposition for the Notion meaning. Fidelity of a
      protocol record and the honesty of a disposition are exactly the
      judgements reserved for Codex.
    what_to_attack: >
      Every requirement in the record against the live CLAUDE.md section
      (any softened step is a defect); whether the Notion row (page
      34967c87082c81ffacfbd04b59df64fe) truly has no live authority; the
      rewritten stage-1 tests in scripts/m2-closure-register.test.mjs for
      weakened invariants; the register arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round, PASS (one nit)
  - id: CX-8
    subject: "PR #509 — cross-surface identity mechanism (G3 stage 1): --git-<sha> record keys, DEC-20260904-B"
    commit: 88d8c0c0
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      Codex mapped the collision identity plan the closure register rests
      on; this batch extends the record-key grammar and the validator's
      derivation rules. A grammar or derivation mistake here propagates to
      every future collision resolution.
    what_to_attack: >
      The record-key pattern in its three copies; the cross-surface
      resolution rule (a git-qualified record AND a gap-report citation,
      formal_record refused); RECORD_GIT_KEY_* and
      RECORD_GIT_KEY_WITHOUT_CROSS_SURFACE; whether decision-records-lib
      accepting --git- keys without the registry opens a hole the closure
      validator does not close; DEC-20260904-B's reversal condition.
    claude_verdicts: 2 rounds (initial + follow-up), PASS
  - id: CX-7
    subject: "PR #503 — G1 rule (DEC-20260904-A): 76 pre-readiness feature-scoped rows become evidence-only"
    commit: 90c1c798
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      A classification rule moved 76 rows out of migration scope in one step.
      The Claude review recomputed the population and digests independently
      and mutation-tested the predicate; the model that set the fidelity bar
      for decision records should confirm the rule's predicate and reversal
      condition are the right boundary, not merely correctly applied.
    what_to_attack: >
      The predicate itself (is "feature-scoped and pre-2026-08-12" the honest
      boundary for evidence-only, or does it sweep in rows with live
      authority?), the 76-row population against the private projection at
      995cece3, the record's Consequences and Reversal sections, and the
      register's arithmetic and digests via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round, PASS
  - id: CX-6
    subject: "PR #502 — capability input-shape guards: wrong-shaped list input must refuse, not crash"
    commit: bd755ea8
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      The Claude review of the first pass was the thing that caught the real
      defect, and that is precisely the argument for a second model here. The
      authoring session fixed the three executors production had exposed and
      wrote a guard test keyed on the `?? []` idiom; the reviewer found that
      two live capabilities carried the identical crash while that guard
      stayed green, and re-scanning on the cast found four more. One green
      gate that could not see its own failure mode is exactly the class
      LESSONS.md F5 tracks, and the widened guard has not itself been read by
      anyone but its author and one Claude reviewer.
    what_to_attack: >
      The regex in apps/api/src/lib/capability-input.guard.test.ts — it is a
      source scan, and its docstring claims only to close the copy-paste path,
      not every spelling. Find a shape it still misses (the destructured and
      aliased locals are named as known gaps; look for others). Then
      country-economic-indicators.ts, the one deliberate behaviour change: a
      non-array `indicators` used to fall through to DEFAULT_INDICATORS and
      bill a successful response about indicators the caller never asked for,
      and now refuses — confirm no caller depended on the old fallback. Also
      check that readArray's `[]`-for-absent contract did not turn a
      previously-thrown "required" error into a silent success at any of the
      12 call sites.
    claude_verdicts: 1 round, PASS WITH FINDINGS, findings acted on (scope widened from 3 files to 9)
  - id: CX-5
    subject: "PR #500 — M2 batch: 2026-08 operating-window rows, seven formal candidate records"
    commit: 7d6343e1
    merged: "2026-09-04"
    priority: high
    status: pending
    why_codex: >
      Decision-record fidelity is exactly the work the 2026-09-01 routing
      reserved for Codex: the doctrine batch failed its first Codex review
      for over-compression, and Codex mapped the collision identity plan
      these records now rely on. A Claude review passed this batch; the
      model that set the bar should confirm it.
    what_to_attack: >
      Each record's five sections against its Notion source for any dropped
      boundary, option or number; the topic split and the four relation
      edges (only source-stated ones are allowed); and the closure register's
      arithmetic and digests, including the private half via
      scripts/m2-closure-verify-private-rows.mjs.
    claude_verdicts: 1 round, PASS, one nit (an accurate forward reference)
  - id: CX-4
    subject: "PR #499 — hono 4.12.8 -> 4.13.5, WP13 batch 1"
    commit: 2a2519f0
    merged: "2026-09-04"
    priority: medium
    status: pending
    why_codex: >
      A framework minor upgrade on the request path of every endpoint. The
      Claude review reproduced the audit numbers and reachability by grep and
      read the changelog; a different model reading the same changelog for
      behaviour this app depends on (c.res reassignment in middleware,
      bodyLimit, cors, HTTPException) is cheap insurance on a change that
      touches everything.
    what_to_attack: >
      apps/api/src/routes/x402-body-limit.test.ts's re-scoped case (is it
      still discriminating, or has it become a tautology now that the
      framework refuses the body itself?), and any hono 4.13 behaviour change
      under the /v1/public/ops/* response-rewriting middleware in app.ts.
    claude_verdicts: 1 round, PASS, two nits applied
  - id: CX-1
    subject: "PR #494 — withdrawn capabilities must not be advertised anywhere"
    commit: 0daa93c9
    merged: "2026-09-03"
    priority: high
    status: pending
    why_codex: >
      The largest single change of the day and the one where a Claude-only
      review is least adequate: five consecutive Claude rounds each returned
      FAIL, and the fourth disproved the fix strategy the third had built.
      That pattern is evidence the surface is hard to reason about, not that
      the fifth round finally exhausted it.
    what_to_attack: >
      apps/api/src/lib/public-ops-visibility.ts and its middleware in app.ts.
      It rewrites every /v1/public/ops/* JSON response. Ask what a withdrawn
      slug can still ride out on — an unrecognised key, a nested details
      blob, a base64 or URL-encoded field, a number-keyed map — and whether
      the prose redaction can corrupt a legitimate payload. Then ask whether
      the guard can drop something a live page needs; the website consumes
      four of these endpoints and no review has loaded a page.
    claude_verdicts: 5 rounds, all FAIL, all findings addressed

  - id: CX-2
    subject: "PR #497 — the session gate stopped instructing removal of live worktrees"
    commit: 1f4339f2
    merged: "2026-09-03"
    priority: medium
    status: pending
    why_codex: >
      It weakens a safety gate on purpose. The reasoning is that the gate was
      wrong about what a state licensed rather than wrong about the state,
      but a second opinion on a check that now fails less often is worth
      having from a different model.
    what_to_attack: >
      Whether the detached-and-dirty exemption lets abandoned worktrees
      accumulate in practice, and whether the 48-hour staleness escalation is
      reachable in a real session's lifetime.
    claude_verdicts: 2 rounds, PASS both, should-fixes applied

  - id: CX-3
    subject: "Retention: durable production-override records ride the compliance window"
    commit: 0daa93c9
    merged: "2026-09-03"
    priority: high
    status: pending
    why_codex: >
      It changes how long personal data can live. Two Claude rounds got the
      predicate wrong in opposite directions — one left records unprotected,
      the next admitted a type carrying admin-typed free text and would have
      extended that field from 180 days to 1095. A third opinion on a
      retention rule is cheap insurance.
    what_to_attack: >
      DURABLE_OVERRIDE_EVENT_TYPES in apps/api/src/lib/data-retention.ts.
      Enumerate every writer of both admitted types, including scripts never
      committed to this repository, and say whether any payload can carry
      personal data. The comment claims two writers; a production row exists
      that matches neither.
    claude_verdicts: caught and corrected twice by review
