# Auto-generated from database on 2026-03-17
# Review and adjust before using as onboarding template

slug: password-strength
name: Password Strength
description: >-
  Analyze password strength: entropy, crack time estimate, character class diversity, common password check, keyboard
  pattern detection. Computed in-process — the password is never sent to a third party. Algorithmic.
category: security
cost_class: free_unlimited
quota_window: none
price_cents: 2
is_free_tier: false
input_schema:
  type: object
  required:
    - password
  properties:
    password:
      type: string
output_schema:
  type: object
  example:
    score: 80
    issues: []
    length: 10
    strength: very_strong
    suggestions:
      - Use at least 12 characters for better security
      - Add uppercase letters
      - Add numbers
    entropy_bits: 29.22
    character_classes:
      count: 2
      digits: false
      special: true
      lowercase: true
      uppercase: false
    crack_time_estimate: 2 years
  properties:
    score:
      type: integer
    issues:
      type: array
    strength:
      type: string
    suggestions:
      type: array
    crack_time_estimate:
      type: string
data_source: Algorithmic (password entropy calculation, no external data)
data_source_type: computed
transparency_tag: algorithmic
freshness_category: computed
maintenance_class: pure-computation
# SA.2b: per-capability PII classification (F-A-003, F-A-009)
processes_personal_data: true
personal_data_categories:
  - sensitive_special
geography: global
test_fixtures:
  known_answer:
    input:
      password: test_value
    expected_fields:
      - field: score
        operator: not_null
        reliability: guaranteed
      - field: score
        operator: type
        reliability: guaranteed
        value: number
      - field: score
        operator: gt
        reliability: guaranteed
        value: -1
      - field: issues
        operator: not_null
        reliability: guaranteed
      - field: issues
        operator: type
        reliability: guaranteed
        value: array
      - field: strength
        operator: not_null
        reliability: guaranteed
      - field: strength
        operator: type
        reliability: guaranteed
        value: string
      - field: suggestions
        operator: not_null
        reliability: guaranteed
      - field: suggestions
        operator: type
        reliability: guaranteed
        value: array
  health_check_input:
    password: test
output_field_reliability:
  score: guaranteed
  issues: guaranteed
  strength: guaranteed
  suggestions: guaranteed
  crack_time_estimate: guaranteed
limitations:
  - title: Evaluates entropy and common patterns only — cannot check against breach databases or custom password policies
    text: >-
      Evaluates entropy and common patterns — cannot check against breach databases or organization-specific password
      policies
    category: coverage
    severity: info
    workaround: Supplement with a Have I Been Pwned API check to verify the password hasn't appeared in known breaches
  - title: The submitted password is recorded on the transaction for 90 days
    text: >-
      Scoring happens in-process and the password reaches no third party, but capability inputs are
      written to the transaction record like every other capability's, and are redacted on the
      standard 90-day schedule. Do not submit a live credential you are not willing to have retained
      for that period. An earlier version of this description claimed the password was not stored;
      that was inaccurate and was corrected on 2026-09-05.
    category: privacy
    severity: warning
    workaround: Score a representative sample or a candidate password rather than a credential already in use.
