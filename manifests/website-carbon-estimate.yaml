# Auto-generated from database on 2026-03-17
# Review and adjust before using as onboarding template

slug: website-carbon-estimate
name: Website Carbon Estimate
description: Estimate CO2 emissions per page view using the Website Carbon methodology. Returns rating A-F, annual estimate.
category: data-extraction
price_cents: 5
is_free_tier: false
input_schema:
  type: object
  required:
    - url
  properties:
    url:
      type: string
output_schema:
  type: object
  example:
    url: https://strale.dev
    rating: A
    methodology: Website Carbon / Sustainable Web Design model
    page_size_kb: 3
    annual_co2_kg: 0.1
    green_hosting: unknown
    page_size_bytes: 3239
    recommendations:
      - This page is already lightweight. Maintain current optimization practices.
    transfer_encoding: gzip
    co2_grams_per_view: 0.0008
    annual_co2_kg_custom: null
    cleaner_than_percent: 100
  properties:
    rating:
      type: string
    page_size_kb:
      type: number
    annual_co2_kg:
      type: number
    co2_grams_per_view:
      type: number
data_source: HTTP fetch + algorithmic estimation (page weight analysis)
data_source_type: api
transparency_tag: algorithmic
freshness_category: computed
maintenance_class: free-stable-api
# SA.2b: per-capability PII classification (F-A-003, F-A-009)
processes_personal_data: false
geography: global
test_fixtures:
  known_answer:
    input:
      url: https://strale.dev
    expected_fields:
      - field: rating
        operator: not_null
        reliability: guaranteed
      - field: rating
        operator: type
        reliability: guaranteed
        value: string
      - field: page_size_kb
        operator: not_null
        reliability: guaranteed
      - field: page_size_kb
        operator: type
        reliability: guaranteed
        value: number
      - field: annual_co2_kg
        operator: not_null
        reliability: guaranteed
      - field: annual_co2_kg
        operator: type
        reliability: guaranteed
        value: number
      - field: co2_grams_per_view
        operator: not_null
        reliability: guaranteed
      - field: co2_grams_per_view
        operator: type
        reliability: guaranteed
        value: number
  health_check_input:
    url: https://strale.dev
output_field_reliability:
  rating: guaranteed
  page_size_kb: guaranteed
  annual_co2_kg: guaranteed
  co2_grams_per_view: guaranteed
limitations:
  - title: Carbon estimates based on page weight only — actual emissions depend on server location and energy grid mix
    text: >-
      Carbon estimates based on page weight and transfer size — actual emissions depend on server location, CDN, and
      energy grid mix
    category: accuracy
    severity: info
    workaround: >-
      Use as a relative benchmark for comparing pages; for certified carbon reporting, use tools like Green Web
      Foundation or Ecograder
