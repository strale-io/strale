# Auto-generated from database on 2026-03-17
# Review and adjust before using as onboarding template

slug: http-to-curl
name: HTTP to cURL
description: >-
  Convert HTTP request parameters to curl command + equivalent fetch/axios code. Supports auth, headers, body.
  Algorithmic.
category: developer-tools
price_cents: 2
is_free_tier: false
input_schema:
  type: object
  required:
    - url
  properties:
    url:
      type: string
    auth:
      type: object
    body: {}
    method:
      type: string
    headers:
      type: object
output_schema:
  type: object
  example:
    url: https://example.com
    method: GET
    curl_command: |-
      curl \
        'https://example.com' \
        -H 'Content-Type: application/json' \
        -d 'This is a test input for automated capability testing.'
    equivalent_axios: |-
      const response = await axios.get("https://example.com", {
        headers: {"Content-Type":"application/json"},
        data: `This is a test input for automated capability testing.`,
      });
    equivalent_fetch: |-
      const response = await fetch("https://example.com", {
        headers: {
            "Content-Type": "application/json"
        },
        body: `This is a test input for automated capability testing.`,
      });
  properties:
    curl_command:
      type: string
    equivalent_axios:
      type: string
    equivalent_fetch:
      type: string
data_source: Algorithmic (HTTP request to cURL conversion, no external data)
data_source_type: computed
transparency_tag: algorithmic
freshness_category: computed
test_fixtures:
  known_answer:
    input:
      url: https://example.com
      body: This is a test input for automated capability testing.
      headers:
        Content-Type: application/json
    expected_fields:
      - field: curl_command
        operator: not_null
        reliability: guaranteed
      - field: curl_command
        operator: type
        reliability: guaranteed
        value: string
      - field: equivalent_axios
        operator: not_null
        reliability: guaranteed
      - field: equivalent_axios
        operator: type
        reliability: guaranteed
        value: string
      - field: equivalent_fetch
        operator: not_null
        reliability: guaranteed
      - field: equivalent_fetch
        operator: type
        reliability: guaranteed
        value: string
  health_check_input:
    url: https://example.com
output_field_reliability:
  curl_command: guaranteed
  equivalent_axios: guaranteed
  equivalent_fetch: guaranteed
limitations:
  - title: Binary payloads and multipart uploads may produce simplified curl equivalents that need manual adjustment
    text: >-
      Converts standard HTTP request formats — binary payloads or multipart uploads may produce simplified curl
      equivalents
    category: coverage
    severity: info
    workaround: For binary payloads, use the --data-binary flag manually and reference the file path instead of inline content
