# Auto-generated from database on 2026-03-17
# Review and adjust before using as onboarding template

slug: linkedin-url-validate
name: LinkedIn URL Validate
description: Validate LinkedIn profile/company/job URL format and check accessibility. Returns URL type, slug, normalized URL.
category: validation
price_cents: 3
is_free_tier: false
input_schema:
  type: object
  required:
    - url
  properties:
    url:
      type: string
output_schema:
  type: object
  example:
    url: https://example.com
    slug: null
    error: >-
      URL does not match a recognized LinkedIn URL pattern. Expected formats: /in/{slug}, /company/{slug},
      /jobs/view/{id}, /posts/{slug}
    url_type: unknown
    accessible: null
    status_code: null
    valid_format: false
    normalized_url: null
  properties:
    slug:
      type: string
    url_type:
      type: string
    accessible:
      type: boolean
    valid_format:
      type: boolean
data_source: Algorithmic (LinkedIn URL format validation, no external data)
data_source_type: api
transparency_tag: algorithmic
freshness_category: computed
maintenance_class: free-stable-api
# SA.2b: per-capability PII classification (F-A-003, F-A-009)
processes_personal_data: true
personal_data_categories:
  - name
  - professional
geography: global
test_fixtures:
  known_answer:
    input:
      url: https://www.linkedin.com/in/test-user
    expected_fields:
      - field: valid_format
        operator: not_null
        reliability: guaranteed
      - field: valid_format
        operator: type
        reliability: guaranteed
        value: boolean
      - field: url_type
        operator: not_null
        reliability: guaranteed
      - field: url_type
        operator: type
        reliability: guaranteed
        value: string
      - field: slug
        operator: not_null
        reliability: guaranteed
      - field: slug
        operator: type
        reliability: guaranteed
        value: string
  health_check_input:
    url: https://example.com
output_field_reliability:
  slug: common
  url_type: guaranteed
  accessible: guaranteed
  valid_format: guaranteed
limitations:
  - title: Validates LinkedIn URL format and structure only — cannot verify the profile exists or is active
    text: Validates URL format and structure — cannot verify the profile exists or is active without LinkedIn API access
    category: coverage
    severity: info
    workaround: Use the validated URL with social-profile-check for existence verification
