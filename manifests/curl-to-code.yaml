# Auto-generated from database on 2026-03-17
# Review and adjust before using as onboarding template

slug: curl-to-code
name: cURL to Code
description: Convert a curl command to equivalent code in TypeScript, Python, Go, Rust, Java, or PHP.
category: developer-tools
price_cents: 5
is_free_tier: false
input_schema:
  type: object
  required:
    - curl_command
  properties:
    curl_command:
      type: string
    target_language:
      type: string
output_schema:
  type: object
  example:
    code: |-
      import requests

      response = requests.get('https://example.com')
      print(response.text)
    notes:
      - "This is a basic GET request. Install requests with: pip install requests"
    language: python
    dependencies_needed:
      - requests
  properties:
    code:
      type: string
    notes:
      type: array
    language:
      type: string
    dependencies_needed:
      type: array
data_source: Claude API (cURL to code conversion)
data_source_type: api
transparency_tag: ai_generated
freshness_category: live-fetch
maintenance_class: commercial-stable-api
# SA.2b: per-capability PII classification (F-A-003, F-A-009)
processes_personal_data: false
geography: global
test_fixtures:
  known_answer:
    input:
      curl_command: https://example.com
      target_language: en
    expected_fields:
      - field: code
        operator: not_null
        reliability: guaranteed
      - field: code
        operator: type
        reliability: guaranteed
        value: string
      - field: notes
        operator: not_null
        reliability: guaranteed
      - field: notes
        operator: type
        reliability: guaranteed
        value: array
      - field: language
        operator: not_null
        reliability: guaranteed
      - field: language
        operator: type
        reliability: guaranteed
        value: string
      - field: dependencies_needed
        operator: not_null
        reliability: guaranteed
      - field: dependencies_needed
        operator: type
        reliability: guaranteed
        value: array
  health_check_input:
    curl_command: https://example.com
output_field_reliability:
  code: guaranteed
  notes: guaranteed
  language: guaranteed
  dependencies_needed: guaranteed
limitations:
  - title: Supports major languages like Python and Go — less common target languages may produce suboptimal output
    text: >-
      Supports conversion to major languages (Python, JavaScript, Go, etc.) — less common languages may have suboptimal
      output
    category: coverage
    severity: info
    workaround: Review the generated HTTP client code against your project's preferred HTTP library and error handling patterns
