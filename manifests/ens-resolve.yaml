# Auto-generated from database on 2026-04-21 (Phase 4b.2 orphan resolution).
# Source: DEC-20260422-A + audit-reports/2026-04-21-phase-4b2-orphan-decisions.csv.
# output_field_reliability fallback-derived from output_schema.properties (DB column was NULL).
# limitations auto-synthesized from maintenance_class (0 active capability_limitations in DB).
# Review and tighten: reliability markers (common → guaranteed/rare) + slug-specific limitations.

slug: ens-resolve
name: ENS Resolve
description: >-
  Resolve an ENS name to its Ethereum address. Supports all ENS names including .eth, DNS names on ENS, and subdomains.
  Also returns avatar URL and resolver contract.
category: web3
price_cents: 2
is_free_tier: false
input_schema:
  type: object
  required:
    - name
  properties:
    name:
      type: string
      description: ENS name to resolve (e.g., 'vitalik.eth')
output_schema:
  type: object
  example:
    name: thisdoesnotexist99999.eth
    address: null
    resolved: false
    resolver: null
    avatar_url: null
  properties:
    name:
      type: string
    address:
      type: string
    resolved:
      type: boolean
    avatar_url:
      type: string
data_source: Public Ethereum RPC (publicnode.com, llamarpc.com)
data_source_type: api
transparency_tag: algorithmic
maintenance_class: free-stable-api
processes_personal_data: true
personal_data_categories:
  - name
  - financial
geography: global
test_fixtures:
  known_answer:
    input:
      name: vitalik.eth
    expected_fields:
      - field: resolved
        operator: is_true
        reliability: common
      - field: address
        operator: not_null
        reliability: common
output_field_reliability:
  name: common
  address: common
  resolved: common
  avatar_url: common
limitations:
  - title: null
    text: >-
      Output reflects Public Ethereum RPC (publicnode.com, llamarpc.com) at query time. Dependent on upstream API
      availability; may degrade if the provider rate-limits or is temporarily unavailable.
    category: availability
    severity: info
    workaround: null
