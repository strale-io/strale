# Auto-generated from database on 2026-03-17
# Review and adjust before using as onboarding template

slug: json-to-csv
name: JSON to CSV
description: Convert a JSON array of objects to CSV format. Supports custom delimiters. Pure algorithmic — instant and cheap.
category: data-processing
price_cents: 2
is_free_tier: false
input_schema:
  type: object
  required:
    - data
  properties:
    data:
      type: array
      description: Array of objects to convert
    delimiter:
      type: string
      description: "Column delimiter (default: comma)"
output_schema:
  type: object
  example:
    csv: |-
      age,city,name
      30,Stockholm,Alice
      25,Oslo,Bob
    columns:
      - age
      - city
      - name
    row_count: 2
    column_count: 3
  properties:
    csv:
      type: string
    columns:
      type: array
    row_count:
      type: integer
    column_count:
      type: integer
data_source: Algorithmic (format conversion, no external data)
data_source_type: computed
transparency_tag: algorithmic
freshness_category: computed
maintenance_class: pure-computation
# SA.2b: per-capability PII classification (F-A-003, F-A-009)
processes_personal_data: false
geography: global
test_fixtures:
  known_answer:
    input:
      data:
        - age: 30
          city: Stockholm
          name: Alice
        - age: 25
          city: Oslo
          name: Bob
    expected_fields:
      - field: csv
        operator: not_null
        reliability: guaranteed
      - field: csv
        operator: type
        reliability: guaranteed
        value: string
      - field: columns
        operator: not_null
        reliability: guaranteed
      - field: columns
        operator: type
        reliability: guaranteed
        value: array
      - field: row_count
        operator: not_null
        reliability: guaranteed
      - field: row_count
        operator: type
        reliability: guaranteed
        value: number
      - field: row_count
        operator: gt
        reliability: guaranteed
        value: -1
      - field: column_count
        operator: not_null
        reliability: guaranteed
      - field: column_count
        operator: type
        reliability: guaranteed
        value: number
      - field: column_count
        operator: gt
        reliability: guaranteed
        value: -1
  health_check_input:
    data:
      - test
output_field_reliability:
  csv: guaranteed
  columns: guaranteed
  row_count: guaranteed
  column_count: guaranteed
limitations:
  - title: Deeply nested JSON objects are flattened with dot notation and may produce very wide CSV output
    text: >-
      Best with flat or shallow JSON arrays — deeply nested objects are flattened with dot notation which may produce
      wide CSVs
    category: coverage
    severity: info
    workaround: Use flatten-json first with a controlled depth parameter to preview the flattened structure before converting
