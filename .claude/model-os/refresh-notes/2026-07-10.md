# MODEL-OS refresh research — 2026-07-10

Generated by `model-os/refresh-research.mjs` (phase (a): deterministic live-API diff).
Ledger: `C:\Users\pette\Projects\wow-core\model-os\routing.json` — last_verified 2026-07-09 (1d ago; stale_after 21d).

**This report never applies itself.** Phase (b) below needs a session with web tools;
the proposal section needs founder ratification before routing.json changes (roster
changes steer every future run — canon-grade).


## Provider: anthropic

Ledger ids: `claude-fable-5`, `claude-opus-4-8`, `claude-sonnet-5`, `claude-haiku-4-5`

Live check: **SKIPPED** — no ANTHROPIC_API_KEY in env and no authed `ant` CLI — live check skipped (normal in sessions with harness-injected auth only)
The phase-(b) checklist below is the ONLY verification path for anthropic this cycle.

### Phase (b) — web-search checklist (for a Claude Code session with WebSearch/WebFetch)

- [ ] WebFetch https://platform.claude.com/docs/en/about-claude/models/overview.md — any Claude model newer than the ledger's last_verified (2026-07-09)? Any status change (preview → GA, like the Fable 5 miss)?
- [ ] WebFetch the Anthropic pricing page / claude-api skill table for current $/MTok on all ledger Anthropic ids.
- [ ] Re-verify pricing lines for: claude-fable-5, claude-opus-4-8, claude-sonnet-5, claude-haiku-4-5 (ledger pricing strings carry "confirmed <date>" — re-stamp or correct).
- [ ] Check for deprecation/retirement notices affecting any ledger id.
- [ ] Check capability re-ranks (benchmarks, vendor claims) that would reorder role ladders — own logged fallback evidence outranks marketing claims.

## Provider: openai

Ledger ids: `gpt-5.5`, `gpt-5.6-sol`

Live check: **SKIPPED** — no OPENAI_API_KEY in env — live check skipped (normal in a local session with only Anthropic auth)
The phase-(b) checklist below is the ONLY verification path for openai this cycle.

### Phase (b) — web-search checklist (for a Claude Code session with WebSearch/WebFetch)

- [ ] WebFetch https://platform.openai.com/docs/models — anything newer than the ledger's tracked ids (gpt-5.5, gpt-5.6-sol)? (Never invent compound ids — the 2026-07-09 'gpt-5.5-codex' fabrication; a model id is real only if the vendor page, the Models API, or a local config shows the exact string.)
- [ ] Check the OpenAI changelog for Codex CLI / plan-limit changes affecting the openai-codex budget lane.
- [ ] Cross-check ~/.codex/config.toml `model =` against the ledger's runtime_bindings (identity vs binding: a moved binding gets a NEW status:unassessed entry + last_seen update — never an in-place id overwrite).
- [ ] Any status:unassessed entries below carry NO capability evidence — assess capability + pricing so the founder can ratify (or decline) ladder slotting.
- [ ] Re-verify pricing lines for: gpt-5.5, gpt-5.6-sol (ledger pricing strings carry "confirmed <date>" — re-stamp or correct).
- [ ] Check for deprecation/retirement notices affecting any ledger id.
- [ ] Check capability re-ranks (benchmarks, vendor claims) that would reorder role ladders — own logged fallback evidence outranks marketing claims.

## Own-evidence sweep (manual prompt for the phase-(b) session)

- [ ] Grep `model-os/FALLBACK-LOG.md` (this checkout AND each consumer repo's `.claude/model-os/` copy) for entries since 2026-07-09 — recurring "wanted X, got Y" rows on the same model are THE re-rank signal. Own evidence outranks marketing claims.
- [ ] Also grep verification_log + session notes for fallbacks that never made it into the log file.

## PROPOSED CHANGES — NOT YET APPLIED

- [openai] ASSESS `gpt-5.6-sol` (status: unassessed) — capability + pricing evidence, then founder ratifies ladder slotting (or declines).

**Ratification rule:** a human (or a taste-canon session acting on explicit founder direction)
reviews this section, edits routing.json by hand, bumps `last_verified`, appends to
`verification_log`, commits, and re-syncs consumers (`sync-model-os.ps1`). This script
NEVER touches routing.json.

<!-- model-os-refresh: findings=yes -->

---

## Phase (b) — EXECUTED 2026-07-10 (session with web tools; founder-directed)

Scope: assess `gpt-5.6-sol` (status: unassessed — the ASSESS proposal above). Live Models
API check SKIPPED again (no OPENAI_API_KEY in this environment) — id reality is instead
confirmed by three independent non-API sources below, which doctrine accepts (vendor page
or local config shows the exact string).

**Identity (CONFIRMED):** exact API ids `gpt-5.6-sol` / `gpt-5.6-terra` / `gpt-5.6-luna`.
Previewed 2026-06-26, GA via API + Codex 2026-07-09 (OpenAI help center; GitHub Copilot
changelog 2026-07-09; the local ~/.codex/config.toml runs the exact string).

**Pricing (sourced, not yet live-API-verified):** Sol $5/$30 per MTok, Terra $2.50/$15,
Luna $1/$6; cache reads -90%, writes 1.25x, 30-min min cache life. Terra is pitched by
OpenAI as GPT-5.5-competitive at 2x lower cost, which puts GPT-5.5 ≈ Sol's price point —
i.e. Sol ≈ same cost as the current assessed model.

**Capability (third-party, first-week):** Terminal-Bench 2.1: Sol 88.8% vs GPT-5.5 83.4%
vs Opus 4.8 78.9% (QCode roundup; direction consistent with the ledger's TB 2.0 numbers).
Artificial Analysis Coding Agent Index: Sol 80 — leads every eval in the index (DeepSWE,
Terminal-Bench v2, SWE-Atlas-QnA); ~$1.04/task, ~10% cheaper per task than Opus 4.8 and
~1/3 of Fable 5; slightly fewer output tokens per task than GPT-5.5. Specs: 1.05M context,
128K max output, new 'max' reasoning effort + ultra mode.

**Caveats (material to a REVIEW lane):** (1) AA notes a small accuracy uplift over 5.5
"coupled with an increase in hallucination rate" — reviews assert findings, so this is the
one number to watch in our own logged evidence (reviewer output is data, never doctrine —
the existing mitigation). (2) SWE-bench Pro / multi-file root-cause numbers NOT yet
published for 5.6 — gpt-5.5's known weakness there is unverified either way for Sol; the
review lane's job (verifying rendered/executed artifacts) leans on the terminal/agentic
strength, which IS verified. (3) First-week benchmarks; no own-evidence rows yet.

**GPT-5.5 status:** still available; no deprecation announced (the 2026-07-23 Codex API
sunsets cover the 5.2/5.3-codex era, not 5.5).

Sources: openai.com/index/previewing-gpt-5-6-sol, help.openai.com article 20001325,
github.blog changelog 2026-07-09, artificialanalysis.ai/articles/gpt-5-6-has-landed,
qcode.cc/en/gpt-5-6-guide, developers.openai.com/api/docs/deprecations.

### Updated proposal (supersedes the bare ASSESS line above — founder ratifies)

- RATIFY `gpt-5.6-sol` as the codex-lane model: status -> current; fill pricing +
  strengths/weaknesses per above; independent-review primary -> gpt-5.6-sol;
  runtime_bindings[codex-cli].assessed_model -> gpt-5.6-sol (binding and assessed model
  re-converge; the -m pin directive disappears); keep gpt-5.5 (still available) as the
  review-lane ladder fallback and in the heavy-analysis/execution/mechanical ladders
  until 5.6 earns those slots on its own evidence.
- OR DECLINE for now: keep the -m gpt-5.5 pin; revisit when SWE-bench Pro numbers or
  own fallback-log evidence lands.
