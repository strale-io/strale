# Archived verification history

`routing.json.verification_log` is the bounded hot window used during normal review. Before
adding an entry that would exceed `policy.json.retention.verification_log_max_entries`, move
the oldest entry here verbatim, oldest first. This archive is synced doctrine and remains
searchable without making every routing load carry the full historical log.

<!-- archived entries below this line -->

2026-07-10 (billing decision): founder DECLINED opt-in metered usage credits for Fable 5 ahead of the 2026-07-12 change (asked directly, answered 'Don't enable — fall to Opus'). taste-canon re-topped to claude-opus-4-8 with ladder [sonnet]; fable status -> unavailable-from-2026-07-12 (identity + assessed evidence retained; no role placement). anthropic-max lane note resolved: no metered spend, weekly-cap framing accurate for the whole active roster. Sessions should run on opus for taste-canon work from now on. Reversal path recorded on the role block.

2026-07-09 (later): two founder-flagged current events, checked via WebSearch rather than taken on faith. (1) Fable 5's GA status has a real backstory the earlier entry missed: shipped 2026-06-09, pulled offline 2026-06-12 by a US export-control order, redeployed 2026-07-01 after Commerce lifted it (Anthropic's own 'Redeploying Fable 5' post) with a new safety classifier. Current 'status: current' / taste-canon-primary stands — but a real, DATED change is coming: after 2026-07-12, Fable 5 stops counting toward weekly plan limits and moves to metered usage credits instead (was: included in the $200/mo Max weekly cap). The anthropic-max budget lane's 'plan' description below is now stale for Fable specifically as of that date — see its updated note. (2) OpenAI publicly launched GPT-5.6 (three variants: Sol/Terra/Luna; Terra pitched as GPT-5.5-parity at half the cost) TODAY per CNBC/Neowin — but checked directly against the live Models API (same key/method refresh-research.mjs uses) and it does NOT appear yet (120 ids returned, same as the earlier same-day check; no id containing '5.6'/'sol'/'terra'/'luna'). NOT added to the ledger — unverifiable-live is not a basis for a model entry, no matter how well-sourced the announcement. This is exactly what the refresh mechanism is for: re-run `node model-os/refresh-research.mjs` (or wait for Monday's scheduled run) once it's actually queryable, and it will surface as a normal new-candidate proposal, no advance/hardcoded knowledge required. Also shipped this session: a relevance filter in refresh-research.mjs (family+recency gates, both self-relative to the ledger's own entries) that cut a 79-line noisy proposal list down to 14 genuinely-relevant candidates — see the script's own header comment for the design.

2026-07-10: identity/binding separation (review convergence: Codex finding, Claude verification, founder-ratified scope). The codex-cli live config moved to 'gpt-5.6-sol' — caught automatically by the resolver at SessionStart, exactly as designed. Instead of overwriting the gpt-5.5 entry's id (which would have silently re-attached gpt-5.5's assessed Terminal-Bench/SWE-bench evidence to an unassessed successor), the ledger now separates model IDENTITY from RUNTIME BINDING: gpt-5.6-sol added as a status:unassessed entry with no role placement; new top-level runtime_bindings block owns the local-config resolution and names gpt-5.5 as the assessed model the roles still route to (invoke explicitly: codex exec -m gpt-5.5). Ratification of 5.6 = phase (b) assessment -> ladder slotting -> flip assessed_model.

2026-07-09 (audit implementation): full-framework audit findings implemented, founder-ratified. (1) Workflow-bypass gate closed — the gate hook now also matches the Workflow tool and blocks scripts calling agent() with no model: routing anywhere (the one ungated fan-out path). (2) billing_watch mechanism added (generic, dated) — first use: Fable 5's 2026-07-12 move to metered credits; the health hook flags it 7 days out and loudly once active, so the no-surprise-bills guarantee doesn't depend on memory. (3) acknowledged_candidates added — reviewed-not-adopted ids stop re-proposing every cycle (anti alert-fatigue); the weekly workflow now updates one standing issue instead of opening a new one per run. (4) FALLBACK-LOG.md created + doctrine wired — the 'wanted X, fell to Y' evidence the refresh loop greps now has a real file that sessions append to. (5) Health hook now prints the per-repo synced-copy age and flags >30d staleness. Billing audit verdict recorded: recurring metered spend ≈ $0 (workflow hits free list endpoints only; all interactive work on subscriptions); idea-lab's dormant agentic crons (daily-run/weekly-run.yml) confirmed keyless and no-op — do NOT set ANTHROPIC_API_KEY as an idea-lab repo secret without meaning to activate them.

2026-07-09: corrected against live evidence, not priors. (1) Fable 5 confirmed GA (this session ran on it end-to-end; Anthropic's claude-api skill lists it in the current pricing/model table as 'most intelligent generally available model... sits above Opus in capability') — flipped from a 'verify-availability' placeholder to taste-canon PRIMARY. (2) codex model id corrected from the invented 'gpt-5.5-codex' to the real string 'gpt-5.5' — confirmed directly from ~/.codex/config.toml (model = "gpt-5.5") and a live `codex exec` probe echoing 'model: gpt-5.5'; local default effort is 'xhigh'. (3) Added sourced strengths/weaknesses for Opus 4.8 vs GPT-5.5 from WebSearch (DataCamp, CodingFleet, MindStudio/DeepSuite comparisons, July 2026): Opus 4.8 leads SWE-bench Pro (69.2% vs 58.6%) and multi-file/root-cause debugging; GPT-5.5 leads Terminal-Bench 2.0 (82.7% vs 74.6%) and near-ties SWE-bench Verified (88.7% vs 88.6%) — i.e. GPT-5.5's edge is agentic/terminal execution, which is exactly the independent-review lane's job (verifying rendered/executed artifacts), not a case for making it a build-primary.

2026-07-10 (gpt-5.6-sol ratified): phase (b) executed same-day on founder direction (full findings + sources in refresh-notes/2026-07-10.md §Phase (b)). Identity confirmed (exact id via OpenAI help center + GitHub changelog + local config; live Models API check skipped — no key in env). Sourced: $5/$30 per MTok (~= gpt-5.5's price point); TB 2.1 Sol 88.8% vs 5.5 83.4% vs Opus 4.8 78.9%; leads AA Coding Agent Index. Founder RATIFIED: sol -> status current, independent-review primary, codex-cli assessed_model (binding re-converged, -m pin no longer needed for reviews); gpt-5.5 -> review-lane fallback (reach via codex exec -m gpt-5.5), keeps its other ladder slots until sol earns them on own evidence. Recorded watch item: AA measured a hallucination-rate UPTICK vs 5.5 — track own review-quality evidence in FALLBACK-LOG/review outcomes. last_verified bumped to 2026-07-10.

2026-07-13 (billing reversal — founder re-ratification): founder stated directly, in-session, that Fable 5 is back in the standard Max subscription (not metered) — the 2026-07-12 metered-credits change no longer applies to this account. This is a founder-reported personal-billing fact, not independently web-verifiable (Anthropic's public pricing pages don't carry per-account plan state); treated as authoritative per the founder-authority category, same standing as the 2026-07-10 decision it reverses. Per that decision's own recorded reversal path ('enable credits + founder re-ratifies the taste-canon role block'), the actual reversal mechanism turned out to be different (Fable re-included in the subscription, not opt-in credits) but the founder's explicit re-ratification is the operative condition either way, and it is satisfied here. Fable's billing_watch removed (the flagged change is no longer active), status -> current, taste-canon primary restored to claude-fable-5 (ladder: [opus, sonnet]), roles_qualified + quality_rank (95, above Opus's 90 per its own assessed strength) added back so the selector/gate route it correctly. Capability evidence (GA 2026-07-09, sourced strengths/weaknesses) was never in question and needed no re-assessment — only the billing/role-placement fact changed. Re-verify at next ledger review (re-confirm the subscription state hasn't reverted).
