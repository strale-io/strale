# Exemptions for check-no-committed-secrets.mjs.
#
# Format:  <repo-relative path> :: <the exact matched text>
#
# The literal is required, not optional. That is the safety property: you can
# only exempt a value you are willing to write down here in plaintext. A real
# credential can never be allowlisted, because writing it here would leak it —
# so the only way past the gate for a real secret is to rotate and remove it.
#
# An entry that no longer matches anything is reported as stale on every run.

# A test asserting that a private-key-shaped value is REFUSED by grant
# verification. The literal is the bare PEM header with no key material.
apps/api/test/autonomous-cannot-write-production.test.ts :: -----BEGIN PRIVATE KEY-----
