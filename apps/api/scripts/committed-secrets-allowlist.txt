# Exemptions for check-no-committed-secrets.mjs.
#
# Format:  <repo-relative path> :: <the exact matched text>
#
# An entry exempts ONE match in ONE file. Requiring the literal is the safety
# property: you can only exempt a value you are willing to write down here in
# plaintext, so a real token cannot be allowlisted without leaking it.
#
# An entry that matches nothing FAILS the build. Otherwise this file is a place
# to park a secret under a path that does not exist -- green today, permanent
# tomorrow. Same for a line missing the " :: " separator.
#
# This file is scanned like any other. Only the literal half of a well-formed,
# non-comment entry is exempt, so a credential pasted into a comment is caught.

# A test asserting that a private-key-shaped value is REFUSED by grant
# verification. The literal is a bare PEM marker with no key material.
apps/api/test/autonomous-cannot-write-production.test.ts :: -----BEGIN PRIVATE KEY-----
