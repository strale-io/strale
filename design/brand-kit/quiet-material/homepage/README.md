# Quiet Material homepage composition study — set aside

**Disposition, 6 September 2026:** the founder rejected this page direction as boring and too far from the unfinished redesign. Do not use this page arrangement or its candidate geometry as an implementation baseline. Resume at [the original-design baseline](../baseline/README.md); [its registry](../baseline/registry.json) owns the current disposition. The source, generated page, original candidate status and verification below remain frozen historical evidence, not current acceptance.

This directory assembles the accepted Quiet Material companions into a bounded responsive homepage study. It is not a production page, a route proposal or token adoption. The study tests one sequence: light split opening, compact open task list, spacious shared-access diagram, direct-text Dusk invitation and a small footer.

## Exact sources

- Copy and section order: `docs/programs/brand-website/homepage-brief.json`
- Reference lock: `docs/research/2026-09-06-homepage-composition.md`
- Density and illustrative invoice fields: `design/brand-kit/quiet-material/patterns/registry.json`
- Navigation behavior: `design/brand-kit/quiet-material/controls/registry.json`
- Page geometry: `design/tokens/candidates/quiet-material-homepage.json`
- Retained type/control values: `design/tokens/candidates/quiet-material-patterns.json`
- Shared-access asset and limits: `design/brand-kit/quiet-material/illustrations/registry.json`
- Opening atmosphere: preserved `hero-folded-light-background` source, copied unchanged to `assets/hero-folded-light.png`
- Invoice document: faithful paper-document composition from the accepted `story.document` fixture in the patterns registry (`Invoice`, `Example Studio`, `INV-1042`, `EUR 480.00`)

`registry.json` binds these sources with canonical-LF SHA-256 for text/SVG and raw SHA-256 for PNG files. `index.html`, `details.html`, `tokens.css` and `manifest.json` are generated by `build.mjs`; edit the brief, registry, token candidate, stylesheet or interactions, then rebuild.

## Deliberate limits

The execution-record section is omitted entirely because successful execution and own-transaction retrieval remain blocked. The invoice/result composition is labelled `Illustrative example`; it contains no status, timer, receipt or live badge. The shared-access illustration is conceptual. The opening raster and retained logo still have pending rights records. No link calls a network route: unqualified study destinations open a local details page that states the intended purpose and current study status.

## Build and verify

From the repository root:

```powershell
node design/brand-kit/quiet-material/homepage/build.mjs
node design/brand-kit/quiet-material/homepage/build.mjs --check
node design/brand-kit/quiet-material/homepage/verify.mjs <playwright/index.mjs>
```

Verification covers 320, 375, 760, 761, 1120 and 1440 CSS pixels; image decode, bundled fonts, horizontal bounds, disclosure keyboard behavior, link destinations, section/density prohibitions, retained-source hashes and generated-output integrity. It exports `output/homepage-desktop.png` and `output/homepage-mobile.png` after checks pass. Browser verification remains bounded Chromium evidence, not cross-browser or screen-reader qualification.

The `contrast` evidence field checks retained palette pairs and every Dusk gradient stop used here. It does not qualify contrast for every rendered selector or hover/focus state. Full rendered-state accessibility qualification remains part of production implementation; this composition also received direct desktop and mobile visual inspection.
