# The opening, followed by useful work

Open [the two-section preview](index.html). This is the current review target: selected hero B with the provisional shorter copy, followed by the first breadth section. The [original B comparison](../hero-comparison/index.html) and [copy-only study](../hero-copy/README.md) remain unchanged as history. No production tokens or final copy are adopted.

Assess the transition from broad atmosphere and a substantial example into an open, lighter layout. The new section has three jobs, each with one benefit, one illustration and one detail link. There is no subtitle or enclosing card around the whole job. Mobile uses a vertical sequence, preserving the same content and links.

The registry-to-profile and invoice-to-fields illustrations share a source/result visual grammar; the input checks use a different arrangement. These are original, editable HTML/CSS illustrative masters, with all new style values in the candidate tokens. They are not screenshots of real output. The registry and linked manifests constrain the task descriptions; they do not prove current production availability or upstream accuracy.

The three job links open local detail previews with working return links. They are placeholders for full category pages in the eventual website. Tools navigation and the hero's Explore tools action lead to the new section. Other inherited navigation and calls to action still lead to the prior review scope; this batch does not implement signup, payments or a live catalogue.

## Sources and reproduction

- [Content and source register](registry.json): the section heading, jobs, benefits, detail copy, accessible descriptions and source manifests.
- [Candidate values](../../../tokens/candidates/quiet-material-opening.json): section geometry and document-object recipes; the existing hero stylesheet and tokens supply fonts, palette and B treatment unchanged.
- [Hero copy](../../../../docs/programs/brand-website/HERO-COPY.md): provisional text; generated rather than copied into a second editable register.
- [Template](style.template.css) and [builder](build.mjs): preserve B's source and append only the first section, plus the three local detail previews.

Run `node design/brand-kit/quiet-material/opening/build.mjs`, then `node design/brand-kit/quiet-material/opening/verify.mjs <absolute-path-to-playwright-index.mjs>`. Run the builder with `--check` for current-source, generated-output, browser-evidence and screenshot bindings. The [verification record](verification.json) covers seven widths, held B dimensions, accessible illustration labels, page bounds, local detail/return links, keyboard menu and no remote requests/browser errors. It does not certify complete accessibility, production routes or every browser family.

Reference lock: selected B and original folded atmosphere; accepted patterns' open reading/composition rules; the existing homepage brief's three concrete jobs; Refero's bundled copywriting guidance. Original imagery and logo rights still follow their existing register. No new third-party imagery, external art direction or customer claims are introduced.
