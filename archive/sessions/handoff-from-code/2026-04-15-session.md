# 2026-04-15 Session Handoff

**Intent:** Work through the 6 Notion to-dos added earlier (P1–P3 mix). Closed 3 of them, progressed 1, 2 remain blocked on Petter.

## Shipped

### 1. Shared LLM name-resolver + polish-company-data fix (commit `4d07453`)
- New utility: `apps/api/src/capabilities/lib/name-resolver.ts` — `resolveCandidate({query, candidates, entityType, hint})`. Uses Haiku to pick best match from registry search candidates; returns `id: null` with reasoning when no confident match. Ready for migration to 11 other registry capabilities.
- `polish-company-data` migrated: collects up to 20 candidates from northdata, delegates ranking, fails loud with candidate list. Also added alias normalization to `extractCompanyName` prompt (e.g. "PKN Orlen" → "ORLEN S.A."). Verified: PKN Orlen, Orlen S.A., Budimex all resolve correctly.
- Bonus: `apps/api/src/db/rotate-test-key.ts` for resetting the test API key (test2@strale.io).

### 2. Double-stringify fix in onboarding pipeline (commit `dc0573d`)
- Root cause: `backfill-output-examples.ts` and `backfill-synthetic-examples.ts` read `output_schema` via raw `db.execute(sql\`...\`)` (bypasses Drizzle's typed parser → returns JSONB as string), then spread `{ ...schema, example }` which char-indexes the string.
- Fix: shared `readJsonbObject()` guard at `apps/api/src/db/lib/jsonb-value.ts`. Parses strings, throws on already-corrupted char-indexed shapes to prevent silent compounding. 6/6 unit cases pass.

### 3. Invoice Verify DE score lift — verified, no action needed
- Step scores pulled: german-company-data (63.7, improving) is the bottleneck; all others 74–98. Solution lift is real.

## Not recovered — escalated

### italian-company-data (SQS 48.9, 0% correctness)
Not recovering passively. Folded into the new migration task *"Migrate registry capabilities to shared name-resolver"* ([Notion](https://www.notion.so/34267c87082c8102973cecd81055a6dc)). italian is the first target — likely the same resolver-bug class, but worth a quick independent diagnosis before migrating (could be selector drift or HTML change instead).

## Blocked on Petter

- **#2 Ireland data source decision** (P1, M) — needs comparison one-pager (current CRO scrape vs. Vision-net, CRO open data, northdata)
- **#3 Denmark data source decision** (P1, M) — needs comparison (cvrapi.dk vs. Virk.dk direct, CVR open data)

Offered to prep both next session. Not yet started.

## Memory updates
- Rotated test API key: `sk_live_0d56f39c...` (old `sk_live_ffcdaa36...` no longer valid)

## Next session suggestions
1. Prep Ireland + Denmark source comparisons (unblocks #2 and #3)
2. Diagnose italian-company-data specifically (pull failing test_executions, inspect outputs)
3. Start resolver migration: italian-company-data first, then the `lib/northdata.ts` shared picker (covers ~7 capabilities in one shot)
